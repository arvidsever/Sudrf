# История roadmap Sudrf

> Архив подробных оснований, проверок и прежних формулировок, вынесенный из
> `Docs/roadmap.md` 29 августа 2026 года. Этот файл не задаёт текущую очередь и статусы:
> для них используются только `Docs/roadmap.md` и актуальные GitHub Issues.

Материал сохранён для проверки происхождения решений. Он может описывать уже завершённые
этапы или состояние на момент переноса; при расхождении действует основной roadmap.

## Полный реестр выпусков

Таблица перенесена из актуального roadmap 10 сентября 2026 года. Она сохраняет
release-level результат, а подробные основания и проверки находятся ниже и в
[`changelog/`](../changelog/).

| Версия | Результат |
| --- | --- |
| 0.3.0–0.9.0 | Базовый поиск и карточка движения, автоматический проход апелляции/кассации, УИД, картотеки суда субъекта и явное состояние CAPTCHA |
| 0.10.0–0.14.0 | Нативный интерфейс и навигация, полный справочник федеральных и военных судов, категории и участники дела |
| 0.15.0–0.20.0 | Отслеживание, первые процессуальные сроки, persistent-кэш и фоновый refresh, строгий TLS, вторая кассация в ВС РФ и пользовательские подборки |
| 0.21.0–0.26.0 | CSV-import, прямые известные карточки и материалы, классификация производств, VNKOD-суды, повторное использование CAPTCHA и Мосгорсуд |
| 0.27.0–0.34.0 | Интерфейс для больших списков, участники УПК/КоАП, параллельный refresh по хостам, уведомления, badge и календарная повестка |
| 0.35.0–0.37.1 | CAPTCHA Assist с очередью по суду и retry, поиск и движение дел мировых судей, исправления ручного flow |
| 0.38.0–0.38.9 | Недельный календарь, автоматическое решение CAPTCHA, Vision-диагностика/preprocess и первая CoreML-модель с локальным корпусом |
| 0.39.12–0.39.29 | Укрепление CAPTCHA state machine, транспорта, model delivery, настроек, диагностики и атомарного накопления корпуса |
| 0.39.30–0.40.4 | Корректная маршрутизация и identity после импорта, восстановление полных цепочек, единый классификатор производств, maintenance-защита и Swift Concurrency |
| 0.41.0 | Swift 6, versioned data layer, Spotlight, App Intents, typed AI pipeline и сводка одного акта; APPLE-6 оставлен на ручной приёмке |
| 0.41.1 | Hotfix падения onscreen activity при открытии дела или акта |
| 0.42.0 | Поиск, карточки и движение Мосгорсуда выверены по живому порталу |
| 0.42.1 | Groq structured output: компактный payload, ограниченный output budget и bounded retry |
| 0.42.2–0.42.12 | Карта репозитория и упрощение структуры, lifecycle/stage fixes, КСОЮ listing, канонические номера и системная локализация календаря |
| 0.42.13–0.42.34 | Серия UI-, lifecycle-, packaging- и Spotlight-исправлений |
| 0.43.0 | Казначейство: все исполнительные документы, строгая RSS-привязка и история исполнения |
| 0.44.0 | ФССП: точный поиск всех ИД, независимый refresh и ручная CAPTCHA |
| 0.44.1 | Developer-only лаборатория CAPTCHA ФССП |
| 0.45.0 | Production-модель CAPTCHA ФССП и ансамбль двух числовых моделей СОЮ |
| 0.45.1–0.45.5 | Строгий allowlist заседаний, TestFlight из main, сортировка мировых участков, локализация и судьи в календаре |
| 0.46.0 | Безопасное удаление пользовательских подборок без удаления дел |
| 0.46.1 | Лицензия CC BY-NC-ND 4.0 и границы сторонних материалов |
| 0.46.2 | Типизированный source-state contract: full, honest zero, partial, CAPTCHA и errors |
| 0.46.3 | Постоянное внутреннее досье, история карточек, номеров и УИДов |
| 0.46.4 | Idempotent startup: identity и проекции не сохраняются без реальных изменений |
| 0.46.5 | #77: диагностика startup store, fail-closed regression и доказанный исторический in-memory fallback |
| 0.46.6 | #195: same-day lifecycle cache и scoped reload устранили блокировку UI при массовом обновлении дел |
| 0.46.7 | #144: retry без изменения файлов и только явный обратимый quarantine неоткрываемой базы с README и Finder |
| 0.46.8 | #44: refresh больше не сообщает об успехе после rollback; движение, identity и key-remap сохраняются одной транзакцией |
| 0.46.9 | #64: fallible persistence commits обязательны во всех mutation paths; rollback блокирует success callbacks, UI и проекции |
| 0.46.10 | #187: календарь снова показывает прошедшие заседания; ближайшее заседание, App Intent и lifecycle остаются future-only |
| 0.46.11 | #79: final response URL определяет host карточек, CAPTCHA и diagnostics; пользовательские WebArchive закреплены обезличенными fixtures |
| 0.46.12 | #78: мировые суды больше не выдают неизвестную или положительную разметку за honest zero; реальная выдача закреплена обезличенной fixture |
| 0.46.13 | #82: ручная CAPTCHA надёжно продолжает исходный поиск; ошибки текущей навигации сразу дают retry, а старые callbacks не повреждают новую попытку |
| 0.47.0 | #91: постоянное меню и нативное подтверждение явно снимают дело с отслеживания; поздние refresh/FSSP callbacks не восстанавливают удалённую запись |
| 0.47.1 | #81: latest-wins загрузка picker не позволяет запоздавшим гарнизонным судам заменить выбранное окружное, апелляционное или кассационное звено |
| 0.47.2 | #117: карточка суда больше не подменяет отсутствующий судебный акт; файловые акты Мосгорсуда показываются честными ссылками |
| 0.47.3 | #210: правая карточка поиска резервирует общую верхнюю полосу и больше не пересекается с глобальным поиском |
| 0.48.0 | #151: открытая карточка и каждая известная инстанция получили точную ссылку на соответствующую карточку суда без fallback на главную страницу |
| 0.49.0 | #213: файловые акты Мосгорсуда в DOC, DOCX и PDF проходят безопасное извлечение и входят в общий просмотр, поиск, Spotlight и AI-контур |
| 0.50.0 | #219: военный поиск больше не требует регион и не предлагает юридически невозможные апелляции на мировых судей; поля и картотеки задаются ветвью и звеном |
| 0.51.0 | #211: «Обзор» показывает номер производства конкретной инстанции у заседаний, событий и опубликованных актов |
| 0.52.0 | #220: регион и суд синхронизируются в обе стороны для субъектового, апелляционного и кассационного звена; ВС РФ и военный поиск остаются без региона |
| 0.53.0 | #110: CSV-импорт завершает работу единым прокручиваемым отчётом с provenance строк, scoped repair, CAPTCHA в том же окне и совместимым экспортом только проблемных записей |
| 0.53.1 | #207: CAPTCHA мировых судей использует ту же рабочую сетевую сессию и cookies, что поиск; скрытый WebView остаётся только у федеральных СОЮ |
| 0.53.2 | #164, этап 1: после принятого ручного кода реальная CAPTCHA мирового суда сохраняется только локально в проверенный SHA-256-корпус; Vision не отправляет коды `msudrf.ru` без отдельной допущенной модели |
| 0.53.3 | #88 + #89: автоматическая CAPTCHA КСОЮ проверяется до ручного fallback; карточка КСОЮ переносит опубликованные этапы жалобы; SUDRF-запросы идут через одну FIFO и не держат больше одного origin; при временно недоступной базовой карточке refresh продолжает вышестоящие цели по сохранённому УИД |
| 0.53.4 | #132: после принятия жалобы КСОЮ показывает присвоенный номер производства 88/88а или 77/77У; сырой номер, URL, identity и навигация не меняются |
| 0.54.0 | #90 + #130: реальная активная карточка КСОЮ по делу КоАП получает отдельную стадию «Надзор» и активное звено «Кассационный суд»; в ГПК/КАС/УПК КСОЮ и судебные коллегии ВС РФ остаются «Кассацией», а редкий Президиум ВС РФ — отдельным «Надзором» |
| 0.55.0 | #94: регистрации одного дела в том же суде объединяются по УИД и официальной ссылке на предыдущую регистрацию; история сохраняет точные карточки, акты и обжалования без tracked-дубликата |
| 0.55.1 | #74: связанные материалы показывают собственное опубликованное движение; временная ошибка карточки сохраняет строку материала и последний успешный кэш |
| 0.56.0 | #92: дело, вышестоящую карточку или материал федерального суда можно добавить в «Мои дела» по прямой ссылке после неперсистентной проверки реквизитов; обычные repair и refresh связывают якорь с логическим делом |
| 0.56.1 | #56: кассация текущего процессуального круга больше не создаёт ложный срок апелляции; недатированный пересмотр не завершает дело и не перебивает датированную апелляцию |
| 0.56.2 | #248, частично: исторические КАС-карточки больше не отображаются как гражданские из-за старых технических `delo_id/new`; исходные параметры URL сохраняются для загрузки карточки |
| 0.57.0 | #70: единый generated registry подключает 66 правил из `Docs/legal-deadlines`; первые шесть typed bindings рассчитываются fail-closed с provenance, а подтверждённые, изменённые и исторические occurrences имеют раздельные status/lifecycle |
| 0.57.1 | #181, уровень 1: единый offline fixture contract проверяет реальные ответы SUDRF, КСОЮ, vintage SUDRF, `msudrf`, Мосгорсуда и ВС РФ от исходных байтов до `CaseMovement`/`SourceAttempt`; отсутствующие реальные captures остаются видимыми пробелами матрицы |
| 0.57.2 | #155 + #181, уровень 2: полный успешный refresh пишет доказанные изменения в append-only shadow-журнал `CaseEvent`; реальные SUDRF, КСОЮ и `msudrf` fixtures проверяют semantic diff, а текущая лента остаётся прежней до #179 |
| 0.57.3 | Неработающие ссылки импортированных дел федеральных судов SUDRF восстанавливаются по проверенным параметрам картотеки либо точному поиску в том же суде; временные ошибки не заменяют адрес и не стирают последний успешный кэш |
| 0.57.4 | #239: краткое название дела КоАП выбирает привлекаемое лицо по опубликованной роли, сохраняя защитника и остальных участников в полной карточке; старые снимки исправляются из сохранённого движения без сети |
| 0.57.5 | #265: карточка жалобы КоАП сохраняет опубликованные результат и дату рассмотрения без строки судебного УИД; завершённые производства уходят из активного «Надзора», а неоднозначные реквизиты остаются fail-closed |
| 0.57.6 | #269: точные апелляционные и кассационные карточки, найденные по УИД, объединяют отдельные досье при подготовке хранилища и частичном обновлении; живая приёмка подтвердила обе пары КоАП, 3 строки надзора вместо 5 и сохранность данных |
| 0.57.7 | #271: восстановление узнаёт принятый номер в полном составном заголовке КСОЮ; живая проверка восстановила 17 из 18 карточек со скриншота, включая четыре контрольные; у 2-770/2019 остаётся неполная выдача, общий refresh восстановленных досье частичный из-за других источников |
| 0.57.8 | #257: ближайшие заседания материалов показываются в «Обзоре» и App Intents независимо от завершения основного дела; номер материала восстанавливается из кэша, календарная история и хранилище сохраняются; живая приёмка подтверждена на 13-3241/2026 |
| 0.58.0–0.58.4 | #224 LegalCalendar, #133 названия дел в «Обзоре», #263 точная связь акта с инстанцией, #261 атомарность событий и #233 фильтры поиска |

## История релизов и проверок

| Версия | Issue | Что оказалось на деле |
| --- | --- | --- |
| 0.41.0 | PR #42, `S6-0B`–`APPLE-6` | Swift 6, versioned data layer, Spotlight, App Intents, typed AI pipeline и сводка акта вошли вместе; APPLE-6 остался на ручной системной приёмке |
| 0.42.1 | PR #49, `AI-4` | Полевой Groq потребовал компактного structured payload, ограниченного output budget и bounded retry вместо ослабления валидации |
| 0.42.13 | #83 календарь | `minHeight` без потолка + `Spacer`, забиравший высоту колонки |
| 0.42.14 | #75 навигация | Капсула не резервировала место; пять экранов гадали отступ → `NavChrome.contentInset` |
| 0.42.15 | #97 сортировка судов | Номер уже лежал в `TerritorialCourt.number` |
| 0.42.16 | #99 ложные заседания | «Есть время ⇒ заседание» + дубль эвристики в ленте. Попутно убран `kind` из `feedID` |
| 0.42.17 | #100 суд инстанции | `nextEventCourt` в presentation. Попутно `recordCourt` — иначе ломался фильтр |
| 0.42.18 | #80 срок от итога | «Последняя строка с непустым результатом» → семантический триггер |
| 0.42.19 | #84 стадия апелляции | Две дыры словаря: возврат жалобы и голое «Прекращено» |
| 0.42.20 | #98 retention сроков | Один предикат вместо трёх копий; в календаре была своя копия условия |
| 0.42.21 | #85 шапка карточки | Стеклянная подложка — выбор автора из четырёх нарисованных вариантов |
| 0.42.22 | #102 карточки дел | Formatter был готов; правка — вёрстка + `categoryTail` |
| 0.42.23 | #96 слайдер навигации | `Glass.identity` + `glassEffectID`; акцент ушёл из подписи, остался на счётчике |
| 0.42.24 | #105 верхняя панель | Поиск глобален — уехал в полосу окна; сортировка потянула карточные виды |
| 0.42.25 | #103 настройки | Пять разделов, а не шесть: «Общие» нечем наполнить |
| 0.42.26 | правки тулбара | Кнопка обновления рисовалась пустой; капча ушла из строки меню; лаг |
| 0.42.27 | упаковка .app | Ресурсные бандлы не клались внутрь **никогда** — приложение падало |
| 0.42.28–30 | слайдер #96 | Три захода: скорость → механика → порядок слоёв |
| 0.42.31 | первый запуск базы | Диагноз не подтвердился: свежая установка работала, у тестировщика база БЫЛА |
| 0.42.32 | иконка (#146) | Артворк из Codex приехал без альфы — сплошной квадрат; пересобран по сетке macOS |
| 0.42.33 | Spotlight (#95) | Три причины: конфликт старых LaunchServices-регистраций, неполный набор fetch-атрибутов и сбой App Entity donation в macOS 27 beta |
| 0.42.34 | срок записей Spotlight (#153) | Явный бессрочный индекс и одноразовый rebuild старого v4-манифеста |
| 0.43.0 | Казначейство (#150) | Все листы из карточки суда, строгая RSS-привязка, история исполнения и независимый refresh |
| 0.44.0 | ФССП (#113), ручной контур | Точный поиск всех ИД по всем регионам, независимый refresh двух источников и ручная CAPTCHA без потери последнего успеха |
| 0.44.1 | лаборатория CAPTCHA ФССП (#160) | Отдельный developer-tool, FSSP-препроцессор 240×80, стабильный SHA-split и fail-closed bootstrap-обучение |
| 0.45.0 | CAPTCHA ФССП и СОЮ | Production-модель ФССП с тремя свежими попытками и ручным fallback; ансамбль двух новых числовых моделей СОЮ |
| 0.45.1 | #124 ложные заседания | Время и результат перестали продвигать строки движения; заседания определяются строгим allowlist поля `event` |
| 0.45.2 | TestFlight-сборка | Sandbox и миграция контейнера вошли в main; archive теперь гарантированно содержит все production-модели CAPTCHA |
| 0.45.3 | #118 сортировка мировых участков | Номер берётся из классификационного кода `NNMSNNNN`; общий порядок больше не сравнивает названия участков |
| 0.45.4 | #127 русская локализация | Удалены девять ошибочных английских переводов русских source strings |
| 0.45.5 | #112 судья в групповом календаре | Судья показан у каждого дела; общий footer оставлен для общих реквизитов |
| 0.46.0 | #114 удаление подборок | Подборки удаляются с подтверждением; сами отслеживаемые дела сохраняются |
| 0.46.1 | лицензия репозитория | Для оригинальных материалов явно опубликованы CC BY-NC-ND 4.0 и границы сторонних лицензий |
| 0.46.2 | #177 source state | Типизированы полные, пустые, частичные и ошибочные ответы; CAPTCHA КСОЮ решается до публикации движения |
| 0.46.3 | #178 identity/reconciliation | Дело получило постоянное внутреннее досье; карточки, номера и несколько последовательных УИДов хранятся с историей |
| 0.46.4 | запуск существующей базы | Identity и проекция актов не пересохраняются без фактических изменений; repair не повторяет стартовое согласование |
| 0.46.5 | #77 startup store | Зафиксированы диагностика открытия persistent store, fail-closed regression и исторический in-memory fallback |
| 0.46.6 | #195 refresh | Same-day lifecycle cache и scoped reload сняли блокировку UI при массовом обновлении |
| 0.46.7 | #144 recovery базы | Добавлены retry без изменения файлов и только явный обратимый quarantine неоткрываемой базы |
| 0.46.8–0.46.13 | #44, #64, #187, #79, #78, #82 | Атомарность persistence, честный refresh, календарь прошедших заседаний, final response URL, honest zero мировых и ручная CAPTCHA |
| 0.47.0–0.47.2 | #91, #81, #117 | Безопасное снятие с отслеживания, latest-wins picker и честные ссылки файловых актов Мосгорсуда |
| 0.48.0–0.52.0 | #151, #213, #219, #211, #220 | Точные ссылки карточек, файловые акты, военный поиск, номера инстанций и синхронизация региона с судом |
| 0.53.0–0.53.4 | #110, #207, #164, #88, #89, #132 | CSV provenance, session CAPTCHA мировых, локальный корпус, КСОЮ CAPTCHA/движение и принятый номер жалобы |
| 0.54.0–0.56.2 | #90, #130, #94, #74, #92, #56, #248 частично | Надзор КоАП, объединение регистраций, материалы, прямые ссылки, lifecycle кассации и классификация исторических КАС-карточек |
| 0.57.0–0.57.8 | #70, #181, #155, #248 частично, #239, #265, #269, #271, #257 | Registry сроков, fixture contract, shadow-журнал, восстановление импортированных карточек, КоАП, объединение досье, КСОЮ и заседания материалов |
| 0.58.0 | #224 | Федеральный LegalCalendar 2013–2026, расчёт процессуальных сроков и UI; исторические и региональные пробелы остаются в issue |
| 0.58.1 | #133 | Названия дел добавлены в сроки и оба вида ленты «Обзора» отдельной проекцией по `recordKey` |
| 0.58.2 | #263 | Точная связь акта с инстанцией имеет приоритет над совпадением уровня; сохраняются документ, текст и сводка |
| 0.58.3 | #261 | Повторные события и rollback журнала исправлены атомарной границей refresh, сроков, CAPTCHA и слияния |
| 0.58.4 | #233, PR #281 | Выбор пяти фильтров применяется после SwiftUI handler; старые запросы инвалидируются, пройдены профильные и полные тесты, Xcode 27 build и живая приёмка |

### Непривязанные наблюдения

- До открытия исправленных карточек общий счётчик изменился 544 → 481, КАС — 230 → 167. Это не результат #265 и не часть массовой приёмки #248; для вывода нужно отдельное сопоставление записей до и после запуска. `MDB_MAP_FULL` исключён: причинная связь не подтверждена.

### Сопоставление ранее собранных локальных версий

| Старое обозначение | Новая версия | Основание |
| --- | --- | --- |
| 0.42.35 | 0.43.0 | Новая пользовательская функция — Казначейство |
| 0.42.36 | 0.44.0 | Новый источник данных — ФССП |
| 0.42.37 | 0.44.1 | Внутренний инструмент обучения; production не меняется |
| 0.42.38 | 0.45.0 | Автоматическая CAPTCHA ФССП и новые production-модели СОЮ |

Main — **0.46.4**, билд 131, 829 тестов. Следующее исправление — 0.46.5; следующая новая пользовательская функция или источник данных — 0.47.0.

### Правила SemVer

- Новая пользовательская функция или новый источник данных повышают второй сегмент и сбрасывают третий в ноль.
- Исправления, косметические изменения, переобучение без новой продуктовой возможности и developer-only инструменты повышают третий сегмент.
- Каждая влитая ветка получает собственный номер, выбранный по этим правилам; отсутствие отдельной сборки не является основанием объединять заметки.

**ФССП #113 работает end-to-end с production-распознаванием CAPTCHA.** Для каждой строки
исполнительного документа поиск идёт независимо по опубликованному судом электронному ИД,
а при его отсутствии — по номеру бумажного листа; регион не ограничивается, чтобы не
потерять переданное производство. Точное совпадение хранит официальные поля ФССП рядом с
независимым результатом Казначейства. CAPTCHA и ошибка одного источника не стирают
последний успех другого. Автор визуально подтвердил живой сценарий
`11RS0001#2-9143/2025#1`: найдено ИП `587893/26/98078-ИП`, предыдущий номер и
подразделение совпали с официальной выдачей; второй ИД независимо получил «не найдено».

В 0.45.0 отдельная CoreML-модель ФССП прошла неизменённый production-gate: 4 000
подтверждённых сервером PNG, 30 вручную проверенных фикстур, 97,2% полных ответов на
независимом экзамене и 99,7% точности у 373 ответов с уверенностью не ниже 90%. Поток
получает свежий challenge на каждой из максимум трёх попыток, а затем оставляет ручной
fallback. Модель опубликована отдельным release asset; чистая CI-сборка проверяет SHA-256,
eligibility-отчёт и наличие модели внутри `.app`.

**Числовая CAPTCHA СОЮ в 0.45.0 переобучена на 16 228 визуально проверенных PNG.** Две
компактные CoreML-модели работают ансамблем: приложение выбирает более уверенный ответ.
На независимом экзамене полностью распознано 91,4% CAPTCHA и 98,2% отдельных цифр; все
известные регрессионные изображения распознаны без ошибок. Обе модели доставляются
отдельными release assets и вместе проверяются в упакованном приложении.

**#124 закрыт в 0.45.1 строгим allowlist заседаний.** Время и текст результата больше не
служат положительными признаками: общий предикат lifecycle, «Обзора», календаря и ленты
смотрит только на подтверждённые формулировки поля `event`. Результат по-прежнему нужен
только для исключения уже завершённых заседаний из будущих; миграция данных не требуется.

**#118 закрыт в 0.45.3 числовым порядком мировых участков.** `MagistrateCourt.number`
берёт номер из фиксированного классификационного кода (`11MS0001` → `1`), а
`SearchModel` передаёт его в уже существующий общий сортировщик. Участки идут как
№ 1, № 2, № 10, № 100; некорректные коды и ненумерованные суды сохраняют алфавитный
fallback.

**#127 закрыт в 0.45.4 удалением ошибочной английской локализации.** Девять русских
source strings в `Localizable.xcstrings` имели `en`-переводы, поэтому macOS показывала
`Open case` и другие английские подписи. Английские юниты удалены; каталог и development
region остаются русскими, без дублирования source strings и без лишнего `knownRegions`.

**#112 закрыт в 0.45.5 привязкой судьи к каждому делу в календаре.** В grouped-блоке
судья теперь показан компактной подписью в той же строке, что время и номер дела, а общий
footer содержит только общие реквизиты суда и помещения. Конфликтные и одиночные блоки
сохраняют прежний состав данных; высота блоков и позиции на временной сетке не изменены.

**#114 закрыт в 0.46.0 удалением пользовательских подборок.** Действие доступно в
контекстном меню подборки и всегда требует подтверждения с числом дел. Удаляется только
членство дел в подборке: сами дела остаются отслеживаемыми, активный фильтр возвращается
к «Все дела», счётчики перестраиваются, а Spotlight обновляется только для затронутых дел.
Системную строку «Все дела» удалить нельзя; пустые подборки удаляются тем же действием.

**TestFlight в 0.45.2 больше не является отдельной линией разработки.** Sandbox,
миграция прежнего контейнера и полный набор production-моделей CAPTCHA влиты в `main`.
Каждая следующая сборка для тестировщиков архивируется из актуального `main` с новым
номером build; постоянная ветка `testflight` не используется. Проверка Xcode-сборки
сверяет SHA-256 всех трёх моделей и eligibility-отчёт внутри фактического `.app`.

**Исполнение в Казначействе #150 завершено сквозным контуром.** Карточка суда извлекает
все строки таблицы исполнительных документов, общий блок «Исполнение» стоит в конце
карточки, а для отслеживаемых дел бумажные листы автоматически проверяются через RSS и
прямую страницу Казначейства. Сохраняются точный официальный статус, орган, подразделение
и история по устойчивым `guid`; not found, ambiguous, terminal и ошибки источника не
стирают последний успех и повторно проверяются по общему TTL. Электронные ИД и бумажные
листы, явно направленные приставам, уже показаны как ФССП, но не отправляются в
Казначейство. Live-кейс `2-8236/2025` подтвердил три независимых бумажных результата,
включая восемь этапов для `ФС № 049377165` (`documentId=2902695`).

**Spotlight в 0.42.33–0.42.34 проверен обоими путями.** Встроенный поиск возвращает дела по
«декабрист», а системный Spotlight видит Sudrf и показывает его записи при фильтрации по
приложению. Общая выдача ранжирует их ниже контактов и файлов — это системный рейтинг, не
потеря индекса. Production-путь теперь использует именованный `CSSearchableIndex` и
`CSSearchableItem` со связанным `IndexedEntity`: так записи остаются доступны во время
известного сбоя `indexAppEntities` / «Failed to request donation» в macOS 27 beta. Записи
получают `expirationDate = .distantFuture`, а переход с manifest v4 на v5 один раз полностью
перестраивает старый индекс, чтобы прежние записи тоже получили новый срок.

**Чужие PR проверять файлами, а не описанием.** В #146 CI был зелёный, описание
перечисляло проверки размеров и `actool` — а PNG не имели альфа-канала вовсе, и иконка
встала бы в доке прямоугольной плашкой. Сборка и подпись проходят с любой формой, поймать
это мог только `sips -g hasAlpha`. Заодно в PR не было промоушен-коммита — черновик
changelog так и остался в `Docs/branch-changelogs/`.
Релиз: https://github.com/arvidsever/Sudrf/releases/tag/v0.42.30 (первый после 0.40.0).

**Слайдер навигации занял три версии, и вот почему.** Ставка на стеклянный морфинг
(`glassEffectID` + `.glassEffectTransition(.matchedGeometry)`) не сыграла: он не перемещает
форму, а гасит её на старой вкладке и зажигает на новой. 0.42.28 замедлял ход — анимировать
было нечего. 0.42.29 перевёл слайдер на `matchedGeometryEffect`, он поехал, но
`GlassEffectContainer` собирает стекло потомков в один слой и кладёт его НАД содержимым,
из-за чего слайдер закрыл подпись. 0.42.30 убрал контейнер. Вывод: у Liquid Glass
семантика слоёв не такая, как кажется по названию API, — проверять сборкой, а не чтением.

**Стекло на стекле теперь есть и никуда не делось.** Трек и слайдер — два слоя без
контейнера, ровно то, что проект себе запрещал в `MyCasesView:44`. Автор принял вид, но
лекарство остаётся в одну строку: снять `.glassEffect` с трека.

Issue закрываю я, после мёрджа и обновления плана. Автозакрытие по телу PR не работает:
GitHub понимает только английские `closes`/`fixes`/`resolves`.

**Подтверждено визуально автором:** сортировка АСОЮ/КСОЮ (#97), суд нужной инстанции у
ближайшего события (#100), высота карточек календаря (#83), шапка выдачи (#75), уход
завершённых апелляций из стадии (#84), миграция `feedID` без ложных уведомлений (#99),
счётчик просроченных сроков (#98, 61 → 3).
Ручная CAPTCHA и два независимых результата ФССП (#113) подтверждены автором на живом
деле после выпуска 0.44.0.
Не проверен только #80 — нужно уголовное дело с приговором.

---

## Подробные наблюдения

Восемь из тринадцати задач потянули находки, которых не было в issue. Трижды это был
фоллаут собственной правки (смена `feedID` воскрешала прочитанные записи ленты; смена
подписи суда ломала поиск по суду первой инстанции; retention сроков заставлял колонку
врать «все сроки подтверждены») — чинилось в том же PR. Дважды — соседний дефект,
вынесенный отдельно (#118, #124). В #102 находка пришла из фикстур: последний раздел
рубрикатора категории часто оказывается заглушкой («иные…», «прочие…»), и наивная свёртка
до последнего раздела дала бы «иные споры по делам о восстановлении на работе» вместо
«Дела о восстановлении на работе». Разделителя в живых карточках два — `→` и `->`.

**Пять раз диагноз расходился с фактом.** В #84 кейс 2 ломался не по описанной причине;
в #104 discovery уже реализован; в #128 моё собственное предположение про материалы автор
опроверг; в #127 строка не «не переведена» — наоборот, переведена; в #143 гипотеза «свежему
пользователю нужна база для миграции» не подтвердилась — пять тестов показали, что первый
запуск работает, а скриншот сам себя опроверг: путь резервной копии в нём появляется только
когда файл базы существовал. Вывод: воспроизводить до правки, не доверять формулировке
причины — и своей догадке тоже.

**Дважды мой собственный вывод оказывался неверен, и оба раза его опроверг автор.**
Про место плана (Bash против адреса файла) и про Liquid Glass («стеклу нечего
преломлять» — у Calendar под капсулой тоже ровный фон, и это не мешает). Проверять
референсом, а не рассуждением.

---

## Swift 6, системные интеграции и AI — snapshot

До 29 августа 2026 года этот раздел жил в отдельном `Docs/AI-ROADMAP.md`.
Теперь выполненные этапы сохраняются здесь как история, а незавершённые встроены
в общую очередь единственного roadmap.

> Все правила, статусы и решения ниже — снимок на 29 августа 2026 года. Актуальные
> формулировки находятся в разделах «Правила ведения» и «Зафиксированные архитектурные
> решения» файла [`Docs/roadmap.md`](roadmap.md).

### Исторический snapshot правил ведения

- Стабильные идентификаторы этапов: `S6-0`, `DATA-1`, `SPOT-2`, `INTENT-3`,
  `AI-4`, `SUMMARY-5`, `APPLE-6`, `CASEAI-7`, `PUBLIC-8`.
- Статусы: `planned`, `in_progress`, `completed`, `blocked`.
- Этап получает `completed` только после merge PR и фиксации результатов
  проверки в этом документе.
- До передачи PR коллеге автор реализации публикует в PR собственный AI
  self-review: риски, проверенные инварианты, результаты тестов и известные
  ограничения. Merge разрешён только после последующего независимого
  adversarial code review коллеги, устранения всех принятых замечаний и
  повторного прогона затронутых проверок. Результаты обоих review и
  перепроверки фиксируются в branch draft и PR.
- Каждая feature-ветка ссылается на идентификатор этапа в своём draft-файле
  `Docs/branch-changelogs/<branch>/vX.Y.Z.md`.
- Release-changelog и номера версий меняются только при merge/release.
- PR #42 является разовым интеграционным исключением: он доводит общий фундамент
  `S6-0B`–`APPLE-6` после нескольких связанных review-pass. После него снова
  действует правило «один этап — один отдельный PR».

#### Обязательный merge-gate каждого этапа

1. Реализация и автоматические проверки критерия готовности.
2. AI self-review автора публикуется в открытом PR раньше review коллеги. Он
   содержит минимум: diff-scope, инварианты, риск потери данных, concurrency и
   privacy, негативные сценарии, результаты тестов и известные ограничения.
3. Независимый adversarial code review коллеги с акцентом на потерю данных, privacy,
   concurrency, ошибочные AI-выводы и деградацию fallback-путей.
4. Разбор замечаний: исправление либо явно записанное обоснование отклонения.
5. Повторный прогон всех затронутых проверок.
6. Merge PR; только после merge статус меняется на `completed`.

### Состояние на момент переноса

| Этап | Статус | Результат |
|---|---|---|
| `S6-0A` | `completed` | PR #39: concurrency-границы CaptchaSolver |
| `S6-0B` | `completed` | PR #42 (v0.41.0): Swift 6 language mode и complete concurrency во всех продуктах и CI |
| `DATA-1` | `completed` | PR #42 (v0.41.0): versioned migration с backup, проекция `CourtActRecord`, стабильные ¶ID |
| `SPOT-2` | `completed` | PR #42 (v0.41.0): инкрементальный Spotlight, deep links, disclosure и opt-out |
| `INTENT-3` | `completed` | PR #42 (v0.41.0): шесть интентов и Shortcuts; системная приёмка Siri/Shortcuts — вручную |
| `AI-4` | `completed` | PR #42 (v0.41.0): typed pipeline, Keychain, cloud-согласие, пилот Groq BYOK |
| `SUMMARY-5` | `completed` | PR #42 (v0.41.0): сводка акта с проверяемыми ¶-цитатами, кэш, stale-индикация |
| `APPLE-6` | `in_progress` | Adapters и translation spike смержены в v0.41.0; ручной go/no-go не пройден |
| `CASEAI-7` | `planned` | После #66, #43, #155/#179 и соответствующей юридической семантики #70 |
| `PUBLIC-8` | `planned` | После #66, #69, source reliability и backend #149 |

### Исторический snapshot продуктовых решений

- Deployment target остаётся macOS 26.0; macOS 27 — активная beta/RC-линия
  разработки. API macOS 27 закрываются availability-проверками.
- Сначала выпускается личный режим с BYOK; ключи хранятся в macOS Keychain.
- Облачная обработка включается разовым явным согласием и может быть отозвана.
- В облако отправляется только акт, для которого пользователь запросил сводку;
  фоновой отправки базы нет.
- Первая генеративная функция — структурированная сводка одного акта с
  проверяемыми ссылками на оригинальные абзацы.
- Системный Spotlight включён по умолчанию; пользователь может отключить его.
  Отключение удаляет индекс Sudrf целиком. Настройка явно сообщает, что индекс
  содержит стороны, реквизиты и полный текст опубликованных актов.
  До первой записи показывается одноразовый экран с заранее включённым
  тумблером: продолжение с ON запускает полный rebuild, с OFF — purge и сохраняет
  отказ. Ранее сохранённое отключение уважается.
- Apple через английский — явно помеченный Experimental-режим, выключенный по
  умолчанию.

### `S6-0` — Swift 6 и macOS 26/27

1. Поднять tools version до Swift 6, включить complete checking и затем Swift 6
   language mode в SwiftPM и XcodeGen; фактический flip фиксировать результатами
   сборки, а не одним статусом в плане.
2. Проверять complete concurrency для всех продуктов и тестов. Для SwiftSoup,
   закреплённого на `2.7.7` из-за бага оптимизатора Swift 6.4 beta, отдельно
   отслеживать upstream и применять `@preconcurrency import` только там, где
   граница действительно нужна.
3. Сохранить изоляцию: UI/SwiftData/настройки — `@MainActor`; сеть, OCR,
   индексация, перевод и AI — actor; межакторные значения — `Sendable`.
4. Поддерживать обязательную стабильную сборку Xcode 26/macOS 26 и сборку
   новых API Xcode 27/macOS 27. Пока hosted runner не содержит Xcode 27,
   стабильная линия блокирует merge, Xcode 27 проверяется вручную, а
   opportunistic CI-job явно сообщает `not executed` и автоматически начинает
   сборку при появлении SDK — без тихой подмены Xcode 26.
5. После macOS 27 RC сделать Xcode 27 основной средой, не повышая deployment
   target до миграции тестеров.

Критерий: package, тесты и схема Sudrf собираются в Swift 6 без concurrency
warnings.

### `DATA-1` — единый слой сущностей

1. До изменения схемы ввести `VersionedSchema` и явный migration plan, сделать
   проверяемый бэкап файлов persistent store (`store`, `-wal`, `-shm`) и при
   ошибке показать блокирующий экран восстановления. Тихая рабочая сессия в
   in-memory fallback запрещена. Marker и каталог backup выводятся из текущего
   `VersionedSchema.versionIdentifier`; если неполной оказалась ранее созданная резервная
   копия, именно она сохраняется в quarantine, после чего создаётся новая проверенная
   копия. Существующая пользовательская база автоматически в quarantine не перемещается:
   это допустимо только по явному действию из #144.
   Переход V1→V3 намеренно проходит последовательно через lightweight-stage V2;
   V2 была внутренним промежуточным состоянием PR и публично не выпускалась.
2. Оставить `TrackedCaseRecord` источником отслеживания, `movementData` —
   источником движения.
3. Добавить перестраиваемую SwiftData-проекцию `CourtActRecord`. `sourceActID`
   считается нестабильной подсказкой, а не гарантией портала. Логический ID
   назначается версионированным алгоритмом и сохраняется: exact source match →
   уникальный semantic match (дело/инстанция/суд/вид/дата) → уникальный
   `sourceHash` → новый deterministic ID. Исправление текста меняет
   `sourceHash` и инвалидирует сводку, но по возможности сохраняет document ID.
4. Вынести общий `ModelContainer`; добавить `CaseCatalog` actor, возвращающий
   только `Sendable`-снимки.
5. `ActParagraphizer` становится единственной границей сегментации и поглощает
   соответствующую логику `CourtActFormatter`; экран, PDF, Spotlight и AI не
   имеют независимого повторного разбиения.
6. Хранить `paragraphizerVersion` и snapshot границ/текстов абзацев вместе с
   актом и сводкой. Обновление алгоритма не перенумеровывает старые цитаты;
   новый snapshot строится только для новой ревизии `sourceHash`.
7. Сохранение основной записи и её проекции остаётся одной транзакцией.
   Refresh, upsert и CAPTCHA перестраивают только затронутое дело; repair удаляет
   старые ключи и строит новый. Полный rebuild остаётся для bootstrap,
   восстановления и явной команды. Если непустой `movementData` не декодируется,
   последняя проекция и сводки сохраняются до успешного обновления; повреждение
   нельзя трактовать как удаление актов.
8. Persistent bootstrap выполняет backup, migration и полную подготовку
   проекции до создания `AppRouter` и вне MainActor. Первый кадр всегда loading;
   при ошибке рабочий UI и mutation paths не становятся доступны.

Критерий: существующая база мигрирует без потери данных, а ID, хеши и номера
абзацев стабильны между запусками.

### `SPOT-2` — Core Spotlight

1. Создать отделённые от SwiftData `CaseEntity` и `CourtActEntity`, реализующие
   `IndexedEntity`.
2. Индексировать реквизиты дела, стороны, категорию, подборки, события,
   результаты и полный текст актов.
3. Реализовать actor `SpotlightIndexer`: insert/update/delete/rebuild.
4. Добавить deep links на дело и выбранный акт.
5. Сначала задействовать системный Spotlight, затем глобальный поиск Sudrf через
   `CSUserQuery`, сохранив нынешний быстрый фильтр списка.
6. Индекс включён по умолчанию с пользовательским opt-out; отключение вызывает
   `deleteAll` независимо от локального manifest. Открытие системного результата
   явно обрабатывается через `CSSearchableItemActionType`. Все writes и manifest
   сериализованы одной FIFO; disable выполняет финальный purge после уже
   начавшейся индексации, а revision не позволяет старому toggle победить новый.
   Fingerprint акта включает денормализованные номер дела и УИД, чтобы их
   точечная смена приводила к реальной переиндексации.
7. Обычная синхронизация получает набор затронутых case keys; debounce объединяет
   наборы, а full scope доминирует. Manifest хранит fingerprint и caseKey акта,
   поэтому reroute удаляет старые документы без обхода всего каталога. Старый
   формат manifest вызывает один purge/full rebuild.
8. До завершения одноразового disclosure indexer не получает writes. После
   onboarding тот же тумблер остаётся в настройках приложения.

Критерий: поиск по реквизитам или смыслу открывает правильные дело и акт.

### `INTENT-3` — App Intents и onscreen awareness

Последовательность интентов: открыть дело, открыть акт, обновить дело, добавить
в подборку, показать заседания, экспортировать акт в PDF.

1. Реализовать `CaseEntityQuery` и `CourtActEntityQuery` через `CaseCatalog`.
2. Добавить русские App Shortcuts с английским fallback.
3. Зарегистрировать URL-схему и обрабатывать deep links/холодный запуск через
   существующий `@MainActor AppRouter`; отдельный `NavigationCoordinator` без
   обоснованного рефакторинга не вводить.
4. Связывать открытые дело и акт с `NSUserActivity` и AppEntity.
5. Русскую печатную Siri AI тестировать экспериментально, не делая её
   обязательным интерфейсом.

Критерий: интенты работают через Shortcuts, корректно обрабатывают CAPTCHA и
сетевые ошибки, а система получает контекст текущей сущности.

Для `RefreshCaseIntent`: при успешном автосолве дождаться обновления; если
CAPTCHA остаётся, вернуть сохранённый кэш и явный результат «требуется открыть
Sudrf», продолжив сценарий только в foreground. Интент не пытается показать
CAPTCHA внутри Shortcuts.

### `AI-4` — универсальная AI-инфраструктура

Базовый интерфейс:

```swift
protocol ActSummarizing: Sendable {
    func summarize(
        document: ActDocument,
        options: SummaryOptions
    ) async throws -> ActSummary
}
```

1. `ActSummary` хранит требования, позиции сторон, обстоятельства, мотивировку,
   резолютивную часть, суммы, даты, сроки, обжалование, предупреждения и ссылки
   на исходные абзацы.
   Предупреждения модели являются теми же cited claims и проходят общую
   валидацию. Provider payload — отдельный тип без `localWarnings`, английской
   диагностики и pipeline flags; эти поля назначает только приложение, в том
   числе для провайдеров без strict JSON Schema.
2. В текущем личном пилоте реализовать только `MockActSummarizer` и Groq BYOK с
   фиксированной моделью `openai/gpt-oss-120b`; Apple подключается тем же
   протоколом как экспериментальный путь. GigaChat и YandexGPT остаются будущими
   benchmark-кандидатами и не входят в настройки, factory или provider adapters
   PR #42. Их добавление допускается только отдельным PR после official-docs gate
   и сравнения качества/географической доступности.
   Каталог [`nejib1/Free-LLM`](https://github.com/nejib1/Free-LLM) используется
   только для discovery кандидатов: сам
   README прямо предупреждает, что тарифы и лимиты меняются еженедельно. Перед
   добавлением каждого нового адаптера обязательна перепроверка по официальным docs:
   фиксируемый model ID, structured JSON/schema, контекст, доступность из РФ и
   региона разработчика, privacy/retention и условия free tier. Community
   routers и провайдеры без понятного оператора/политики обработки судебных
   актов не допускаются. OpenRouter free-router не используется. Google AI
   Studio, Cerebras и Mistral можно включить в benchmark опубликованных актов только
   после этого gate; локальные Ollama/LM Studio из списка рассматриваются как
   прототипы, а production local-путь для macOS остаётся MLX/Core ML.
3. Первый личный прогон фиксирует Groq BYOK и `openai/gpt-oss-120b`: Settings →
   AI содержит Keychain, проверку соединения, облачное согласие и подготовку
   Apple через английский. Выбор провайдера и автоматический режим возвращаются
   только после сравнительного benchmark.
   На 20 июля 2026 года официальная документация Groq подтверждает strict
   structured outputs для этой модели:
   <https://console.groq.com/docs/structured-outputs>. Model ID не выбирается
   через случайный router.
   Первый полевой прогон выявил два `json_validate_failed` на реальном акте при
   успешной синтетической проверке ключа. Corrective-путь использует компактный
   provider payload `items[]` с enum раздела, `reasoning_effort=low`, выводимый
   output budget и не более одного повтора на chunk. Полевой connection probe
   показал, что резерв `8192` output tokens немедленно отклоняется free-tier
   лимитом Groq как HTTP 413 / `rate_limit_exceeded`: минутный token budget
   списывает резерв ещё до инференса.
   Поэтому резерв не задаётся порогом по символам — такой порог при
   фиксированном резерве заведомо допускает запросы, которые не укладываются в
   бюджет. `GroqTokenBudget` оценивает вход (консервативно 2 символа на токен
   плюс служебные токены prompt и схемы) и отдаёт остаток бюджета под вывод,
   не более `4096`; если полезного места не остаётся, поле не отправляется и
   действует server-side completion cap Groq. Обрезанный ответ распознаётся по
   `finish_reason=length` отдельной ошибкой, а не как невалидный JSON.
   Немедленный повтор после `json_validate_failed` разрешён только тогда, когда
   два запроса подряд укладываются в тот же минутный бюджет: на крупном chunk
   повтор всё равно упёрся бы в лимит и отнял бы бюджет у остальных chunks.
   Groq chunk остаётся 18000 символов: проверочный акт на 7564 input tokens
   помещается в контекст одним запросом, но вместе с completion превышает
   бесплатный минутный token budget. Искусственное дробление не решает это без
   минутной паузы между частями: второй chunk получает 429. Это отдельное
   ограничение free tier, не возврат `json_validate_failed`; PR не маскирует его
   бесконечными повторами и не ослабляет structured output.
   Из HTTP body разрешено извлекать
   только allowlisted machine code; provider message и failed generation не
   сохраняются и не выводятся. Проверка соединения обязана получить непустую
   cited summary для многоабзацного синтетического акта.
   Смена `promptVersion` или `pipelineVersion` делает сохранённую сводку
   устаревшей наравне с изменением текста акта: staleness сравнивает и hash
   источника, и идентичность prompt/pipeline.
4. Фиксировать model IDs; не использовать случайный free-router.
5. Benchmark на 50–100 уже опубликованных и обезличенных судом актов хранится
   вне Git; в Git остаются runner и синтетические фикстуры. Повторное
   обезличивание не выполняется: проверяется реальный опубликованный текст.
   Полный benchmark вручную, не в CI.
6. Согласие на внешний cloud уже в личном BYOK-режиме явно предупреждает об
   отправке ФИО и иных персональных данных третьих лиц.

Порог допуска: 100% существующих paragraph-ссылок, не менее 95% правильных
критических реквизитов и 90% полноты обязательных разделов.
Методика, формат фикстур и границы ручного корпуса остаются в
[`Docs/AI-BENCHMARK.md`](AI-BENCHMARK.md); это проверочный материал, а не второй roadmap.

### `SUMMARY-5` — сводка одного акта

1. Добавить действие к выбранному акту и в отдельное окно.
2. Короткий акт обрабатывать одним typed/structured запросом; длинный —
   иерархически по границам абзацев. Chunk budget является свойством модели;
   Apple-путь проектируется под малое контекстное окно и никогда не получает
   типичный длинный акт целиком.
3. Каждый чанк отдельно проходит provider → validation и получает единый бюджет
   не более двух provider-вызовов: один исходный и максимум один повтор либо
   после validation failure, либо после кратковременного 429/5xx. `Retry-After`
   принимается не более 15 секунд; более длинный лимит завершается понятной
   ошибкой. Итоговая объединённая сводка проверяется против полного документа
   без повторной генерации всех чанков. Дословность чувствительна к регистру
   (нормализуются только пробельные символы), а критические реквизиты ищутся с
   границами токенов внутри каждого процитированного абзаца отдельно. Формы
   календарной даты сопоставляются по day/month/year: словесный месяц может быть
   подтверждён числовой датой, но не другим месяцем или годом.
4. Ссылки переходят к оригинальному абзацу с подсветкой.
5. `ActSummaryRecord` хранит provider/model/promptVersion/pipelineVersion,
   `sourceHash` и `generatedAt`; при смене текста сводка устаревает.
6. Всегда показывать предупреждение о проверке по оригиналу.

### `APPLE-6` — Apple Foundation Models и двойной перевод

Режимы: Apple с русским напрямую; Apple через английский (Experimental);
внешний BYOK.

Прямой путь используется только при доступности модели и официальной поддержке
русского locale.

Переводной путь сначала имеет статус spike с go/no-go по отдельному
мини-benchmark. Полный production pipeline ниже реализуется только при `go`.

Spike проверяет загрузку языковой пары, сохранность paragraph/literal IDs,
качество ru→en→summary→ru и реальную вместимость chunk pipeline на нескольких
коротких и длинных актах.

Разрешение на загрузку языков запрашивается только через привязанную к Settings
SwiftUI `translationTask`, последовательно для ru→en и en→ru. Генеративный actor
создаёт headless-сессии только для уже установленных языков и проверяет
`isReady`; UI-сессия за пределами lifetime View не сохраняется. Literal IDs
сравниваются как multiset: грамматическая перестановка разрешена, потеря,
дублирование и подмена отклоняются.

До решения `go` английская сводка валидируется против переведённых абзацев, но
русская citation указывает на исходный абзац целиком: надёжного обратного
сопоставления короткой английской цитаты с русским фрагментом spike пока не
обещает. UI-метка Experimental и обязательная проверка оригинала сохраняются.

Production-путь после `go`:

1. Проверить и при необходимости загрузить пару русский ↔ английский через
   Translation framework.
2. Извлечь из русского оригинала защищённые литералы: номера дел/УИД, даты,
   сроки, суммы, валюты и нормы права; назначить им стабильные IDs.
3. Перевести абзацы batch-запросом, сохраняя paragraph IDs вне переводимого
   текста.
4. Получить строгую английскую `ActSummary` от Apple.
5. Перевести обратно только человекочитаемые поля; JSON-ключи, IDs и реквизиты
   через перевод не пропускать.
6. Подставить оригинальные русские литералы, привязать выводы к русским
   абзацам и выполнить локальную проверку.
7. Показывать индикатор двойного перевода и диагностическую английскую сводку.
8. Кэш учитывать OS build, языковую пару и версию pipeline.

До сравнительного benchmark рабочий пользовательский маршрут зафиксирован на
Groq BYOK / `openai/gpt-oss-120b`. Apple напрямую и через английский существуют
как изолированные экспериментальные адаптеры и не подменяют рабочий провайдер.
После benchmark планируемый автовыбор: Apple напрямую → Apple через английский,
если эксперимент включён → победивший BYOK → понятное сообщение о недоступности.

`APPLE-6` не становится `completed` вместе с merge PR #42: для этого обязателен
ручной translation go/no-go на macOS 26 и macOS 27. Этапы `S6-0B`–`SUMMARY-5`
исторически получили `completed` после своих записанных проверок; незавершённым системным
gate остаётся только APPLE-6 и его отдельные runtime follow-ups.

### `CASEAI-7` — AI уровня всего дела

Статус: `planned`; этап начинается после #66, #43, #155/#179 и юридической
семантики #70.

1. Детерминированное «Что изменилось» между двумя актами.
2. История по инстанциям на основе движения и проверенных сводок.
3. «Спросить по делу»: локально выбрать релевантные абзацы и передать только их.
4. Digest: детерминированные события, AI только поверх проверенных фактов.
5. Natural-language command bar выполняет действия через App Intents.
6. На macOS 27 использовать `SpotlightSearchTool` с fallback для macOS 26.

### `PUBLIC-8` — публичное распространение

Статус: `planned`; этап начинается после #66, #69, базовой source reliability
и backend foundation #149.

1. Сохранить BYOK как пользовательский вариант.
2. Продуктовый облачный режим провести через backend proxy с авторизацией,
   квотами, бюджетами и аудитом без хранения текстов.
3. Провести privacy- и юридическую проверку актов с персональными данными.
4. Собирать только технические метрики без текста и реквизитов дела.
5. Apple через английский сохраняет Experimental до успешного benchmark после
   macOS 27 RC.

### Сквозные проверки

- Миграция SwiftData и стабильность ID/хешей/абзацев.
- Spotlight lifecycle и deep links.
- App Intents, холодный запуск, CAPTCHA и сетевые ошибки.
- Keychain, отзыв согласия и отсутствие секретов в логах.
- Structured output, chunking, retry, отмена и инвалидирование кэша.
- Translation round trip для сумм, дат, статей, имён и номеров дел.
- Сохранность paragraph IDs и literal IDs.
- Сетевой тест: провайдер получает только выбранный акт.
- Матрица Apple Silicon/Intel, macOS 26/27, Apple Intelligence и языковых
  пакетов. На Intel отсутствие Apple Intelligence и деградация к BYOK являются
  ожидаемым результатом. Translation/Apple Intelligence и полный benchmark —
  ручные проверки, а не обязательные jobs CI.

---

---

## Решения автора от 23 августа 2026 года

**#155 — поднять в высокий приоритет.** После ближайшего блока correctness/data-safety,
не дожидаясь закрытия всего накопленного UI/reliability backlog, реализовать #155 в shadow
mode. Следующие крупные функции по возможности строить уже поверх event journal, а не
размножать текущие независимые эвристики изменений.

**#148 Apple Calendar — первая версия только Sudrf → Calendar.** Судебное состояние и
`CaseEvent` остаются source of truth. Изменение или удаление `EKEvent` пользователем не
меняет snapshot/CaseEvent; Calendar — проекция. Двустороннюю синхронизацию в первый scope
не включать. Если позже понадобятся пользовательские заметки/override, хранить их отдельным
локальным слоем, не смешивая с данными суда.

**#149 сервер + APNs — обязательная часть продукта, но позже.** Это не условная задача
«если локального background refresh окажется мало»: always-on backend точно строим, но
только после стабилизации локального event contract #155 и базовой source reliability.
Сервер не заводит вторую доменную модель: он воспроизводит/потребляет те же normalized
snapshots и semantic `CaseEvent`, а затем масштабирует scheduling/delivery.

---

---

## Open-source review и архитектурный audit trail

Подробная фиксация: `Docs/architecture/open-source-reference.md` и
`Docs/architecture/reference-alexxmirny-sudrf-parser.md`.

Главный вывод — **не менять стек**, а сделать явными контракты между уже существующими
слоями. Целевая цепочка:

`transport → source adapter → normalized snapshot → identity/reconciliation → semantic diff → append-only CaseEvent journal → projections`.

### Архитектурные invariants

- **Swift/SwiftUI/SwiftData остаются.** SwiftData не менять без измеренного bottleneck;
  сначала добавить логические сущности snapshot/event/refresh-attempt и нормальные
  миграции.
- **HTTP-first.** `URLSession` + cookies/session continuity + CP1251 + trust roots +
  throttle/retry/backoff — штатный transport. Selenium/Puppeteer/browser automation —
  только fallback для источника, который объективно нельзя получить обычным HTTP. Для
  `msudrf` #164 это означает сначала реализовать KCAPTCHA как HTTP session-unlock, а не
  сразу переносить Puppeteer/WKWebView-подход референсов.
- **Transport не знает процессуальной семантики.** Его ответственность: HTTP, redirects,
  charset, TLS, cookies, throttling, retry, CAPTCHA continuation, host health. CAPTCHA
  protocol при этом может быть token-based или session-based — транспорт обязан сохранить
  это различие, а не свести всё к фиктивному `captchaid`.
- **Source adapter не знает UI.** Он строит URL, классифицирует response, извлекает
  source-native identity и нормализует listing/detail в общую модель. По мере добавления
  #106/#107/#108 вводить capabilities/adapter registry вместо разрастающихся `if source`.
- **Domain не зависит от presentation.** Identity/reconciliation, стадии, сроки и semantic
  diff живут вне View/notification formatter.
- **Номер дела — атрибут, не primary key.** Court identity — официальный код, где он
  надёжен; карточка/производство — УИД либо source-native ID, scoped по court/source;
  logical case — граф связанных карточек. #94/#132/#156 — обязательные regression cases.
- **Последний хороший snapshot не стирается плохим fetch.** HTTP 200 stub, CAPTCHA,
  partial/unknown response и parse error — отдельный refresh outcome, а не новое пустое
  состояние дела.
- **Raw JSON diff запрещён как пользовательская семантика.** `alexxmirny/sudrf_parser`
  полезно подтверждает модель snapshot→diff→notification, но его positional array diff
  — negative reference: перестановка строк/косметика не должны создавать события.
- **Все downstream-проекции читают один event contract.** Лента, локальные notifications,
  badges, Spotlight, Calendar и будущий APNs не сравнивают snapshots самостоятельно.

### Фазы и gates

**A. Надёжный source state до semantic events: контракт #177 завершён в 0.46.2;
source-specific gates продолжаются.**

Закрыть/стабилизировать source correctness и partial-state кейсы, которые могут сделать
новый snapshot ложным: #82/#88/#89/#164, #79/#78 и связанные фикстуры.
Не требуется ждать всех UI/reliability issues; нужен именно пригодный normalized input.

Для `msudrf` gate включает не только распознавание `kcaptchaForm`, но и проверяемый
continuation contract: challenge page → `/captcha.php` → POST `captcha-response` →
session unlocked → исходный listing, всё в одной cookie session. CAPTCHA failure не равна
нулевой выдаче.

Gate: один и тот же response детерминированно даёт usable normalized snapshot,
operation-specific honest-zero outcome либо явный partial/error outcome; не существует
пути «плохой HTML → пустой valid snapshot».

Реализация #177 использует общий `SourceOutcome` до persistence: полный snapshot,
honest zero, partial, CAPTCHA, maintenance, transport и parser failure имеют разные
типизированные исходы. Последний безопасный `SourceAttempt` хранится отдельно от
`movementFetchedAt`; partial применяет только пригодную часть через существующий
`MovementCachePolicy`, но не продлевает TTL полного успеха. Runtime CAPTCHA URL остаётся
в памяти и не попадает в сохраняемый provenance.

**B. #178 — identity/reconciliation и discovery evidence: завершено в 0.46.3.**

Использовать official court code + УИД/source-native IDs и хранить смену display number как
историю identity, а не как новое несвязанное дело. #156 добавляет `r_juid` как независимый
registry/evidence source для передачи по подсудности; #87 использует те же reconciliation
правила при фоновом discovery. Manual и periodic discovery обязаны сходиться к одной цепочке.
#178 реализуется вместе с regression-stories #94/#132/#156, а не после их отдельных
one-off identity paths.

Gate: повторное обнаружение той же карточки другим маршрутом не создаёт дубль; новая
карточка с новым номером может быть связана без опоры на строковое равенство номера.
#155 потребляет identity карточки из #178, но остаётся владельцем identity событий.

Реализация хранит отдельный UUID логического досье, точные source-native карточки,
историю номеров и несколько судебных УИДов с provenance. Совпадение полного структурно
корректного судебного УИДа достаточно для объединения карточек; новый УИД присоединяется
только по официальной predecessor/registry/source-native связи. `case_uid` портала
хранится отдельно и никогда не сравнивается с судебным УИДом. Ручное добавление,
background refresh и repair прежней базы проходят через один reconciler; V5 → V6
сохраняет неизменяемый technical key и старые адреса как aliases.

**C. #155 — shadow `CaseSnapshot → CaseEvent`.**

**Приоритет подтверждён автором 23.08.2026:** эту фазу брать сразу после ближайшего блока
correctness/data-safety, не дожидаясь закрытия всего UI/reliability backlog.

Сохранять последний normalized snapshot и append-only journal, но первое время не менять
действующую ленту. На каждом refresh считать semantic events параллельно и сравнивать с
текущим поведением. Event ID стабилен между relaunch/refetch и пригоден для дедупликации
локального и будущего серверного notification path.

Gate: identical snapshot → 0 events; reordering/whitespace/publish timestamp → 0 events;
новое заседание/акт/инстанция → ровно одно событие; partial fetch → 0 ложных cancel/delete.

**D. #179 — Event journal становится источником пользовательских изменений.**

После shadow-сверки перевести на journal ленту и локальные notifications, затем badges и
прочие производные. Старые presentation strings не участвуют в event identity. Историю
не пересобирать заново при изменении formatter, иначе старые уведомления «воскреснут».

**E. #70 подключается как юридическая семантика поверх того же contract.**

Rules engine владеет формулой срока, trigger/evidence и provenance; #155 владеет общей
семантикой события/dedup/projections. Deadline lifecycle (`proposed/confirmed/changed/
expired/superseded` — финальный vocabulary определить в #155/#70) не заводит отдельный
feed-specific ID.

**F. #148 Calendar — односторонняя projection, не второй database.**

**Решение автора 23.08.2026:** первый scope — только Sudrf → Apple Calendar. После event
identity маппинг `CaseEvent/event state ↔ EKEvent` становится технически прямым: перенос/
отмена обновляют существующий Calendar event. Обратное изменение `EKEvent` не меняет
судебное состояние в Sudrf; двустороннюю синхронизацию в этот этап не включать.

**G. #180 — refresh health, scheduler и canary после устойчивой семантики state/event.**

#87/#68/#65 должны различать `lastAttempt` и `lastSuccess`, не считать partial успешным
refresh и давать evidence, почему источник не обновился. Только после этого имеет смысл
адаптивный scheduler: приоритет по близости заседания/срока, свежим изменениям, ожиданию
higher instance, предыдущим ошибкам, TTL и недавнему открытию пользователем; для внешних
сбоев — exponential backoff + jitter и при необходимости host circuit breaker.

#180 заведён заранее как evidence-first backlog, но реализация начинается только если
измерения после #87 и host-health instrumentation из #68 подтвердят starvation или лишнюю
нагрузку простого TTL. Полный Diagnostics UI #68 scheduler не блокирует. Если проблема не
подтвердится — issue закрывается отчётом без новой очереди.

**H. Новые source families — поверх contract, а не рядом с ним.**

#106 Москва → #107 СПб → #108 общий модуль. Новый источник обязан дать capabilities,
source identity, normalized snapshot и fixture suite; UI не получает новый `if Moscow`
или `if magistrate`, если различие можно выразить adapter capability.

**I. #149 backend — обязательный поздний этап того же contract.**

**Решение автора 23.08.2026:** always-on backend и APNs точно входят в продукт, но позже.
После стабилизации #155 и базовой source reliability сервер масштабирует ingestion/
scheduling/delivery, не вводя вторую модель дела и второй diff-engine. Local refresh и
backend должны выдавать совместимые event IDs/semantics, чтобы одно изменение не приходило
пользователю дважды. PostgreSQL/Redis/queues/search index появляются только при серверной
нагрузке, которая их обосновывает; в desktop их нет.

### #181 Fixture/test contract

Для каждого важного portal family нужны **два независимых уровня regression fixtures**:

1. `HTML/response → normalized snapshot`;
2. `old snapshot + new snapshot → expected CaseEvent[]`.

Постоянный pathology corpus: CP1251; canonical/alternate host; CAPTCHA и post-CAPTCHA
session; **`msudrf` KCAPTCHA (`kcaptchaForm` + `/captcha.php` + POST + cookie continuity)**;
maintenance/error page с HTTP 200; honest zero-results против unknown; duplicate rows;
missing/unstable UID; renumbering; partial KSOYU; stale links; vintage SUDRF;
Moscow/non-standard magistrates; `r_juid` с уже известной карточкой, новой карточкой и
временной пустой/ошибочной выдачей.

#65 live canary ловит внешний drift; fixtures не дают сломать уже исправленное поведение
при адаптации к этому drift. Для semantic event rule обязательны positive **и negative**
fixtures: ложное юридически значимое событие опаснее пропущенного cosmetic change.

### Что брать из reference projects

- **Juriscraper** — adapter/fixture discipline и отделение scraper от persistence/app.
- **CourtSniffer** — актуальные SUDRF protocol quirks, official court code, adapter registry,
  CAPTCHA/session observations; для `msudrf` особенно ценен принцип сохранения browser/
  HTTP session после CAPTCHA. Не переносить Puppeteer/RuCaptcha/TLS bypass как default —
  #164 сначала проверяет тот же протокол чистым HTTP.
- **tochno-st/sudrfscraper** и **dataout-org/sudrfparser** — corpus/legacy oracle, не stack.
- **OlegSirik/sudrf-proxy** — `discovery → local snapshot → refresh → diff → changes` как
  product mental model.
- **alexxmirny/sudrf_parser** — свежий минимальный `fetch → snapshot → diff → changelog →
  notification`, `r_juid` и content/header-based parsing; raw field/index diff и URL-hash
  identity использовать только как антипример.
- **CourtListener** — future server-scale ingestion/workers/search reference, только для #149.

### Явно не делать

- не replatform-ить рабочий native client на Python/Node/Java;
- не делать browser automation штатным транспортом обычных `sud_delo` страниц;
- не отключать TLS verification ради проблемного суда;
- не считать CAPTCHA/error/unknown HTML за `0 results`;
- не использовать human case number или URL как identity;
- не строить feed/Calendar/server notifications через независимые diff-алгоритмы;
- не переписывать SwiftData до измеренной проблемы;
- не тащить Elasticsearch/Redis/Celery/queue infrastructure до server-scale workload;
- не ставить LLM/AI в critical path процессуального статуса, identity или срока.
