# A11 — атомарная пара captcha для корпуса

## Исправлено

- Bootstrap корпуса сохраняет PNG вместе с тем токеном, который был
  отправлен на сервер, а не перечитывает mutable token store.

## Проверка

- Добавлен regression test: перезапись token store после submit не
  меняет разметку PNG в корпусе.
