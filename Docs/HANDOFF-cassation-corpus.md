# Хендофф: правовая база судебных актов КСОЮ

Документ для продолжения работы на машине с прямым доступом к `*.sudrf.ru`.

**Задача 1 (живая разведка) закрыта 06.08.2026.** Результаты вошли
в `Docs/architecture/ksoyu-listing-grammar.md` — теперь это проверенная грамматика,
а не гипотеза; фикстуры и регрессия — `Tests/SudrfKitTests/KSOYuListingGrammarTests.swift`.
Ниже раздел «Задача 1» оставлен как отчёт, остальные задачи актуальны.

**Требуется:** Mac с Xcode 26 (для `swift test`) и сеть без ограничений на `sudrf.ru`.

## Цель проекта

Собирать с сайтов девяти кассационных судов ОСЮ судебные акты и строить по ним правовую
базу с поиском: полнотекстовым, по реквизитам, по цитируемым нормам и семантическим.
Пилот — один КСОЮ, затем масштабирование.

**Целевая архитектура** (решения приняты, обсуждать заново не нужно):

- харвестер, база и API — на российском VPS, Python;
- Postgres 16 + `pgvector` + `pg_trgm`; полнотекст на `to_tsvector('russian')`;
- слой сырых HTML (`raw/<домен>/<год>/<id>.html.zst`) как источник истины — парсеры здесь
  заведомо будут дорабатываться, и архив избавляет от повторного обхода судов;
- капча — CNN из проекта `sudtudtestbot` (использование разрешено автором);
- `Sudrf` (это приложение) остаётся интерактивным инструментом юриста и позже может стать
  клиентом того же API. Сбор в него не встраивается.

## Что уже сделано

PR [#58](https://github.com/arvidsever/Sudrf/pull/58), ветка
`claude/cassation-court-acts-database-xwrhwx`, CI зелёный:

- `Docs/architecture/ksoyu-listing-grammar.md` — грамматика сплошного перечня дел КСОЮ
  с провенансом и статусом проверки каждого параметра. **Это главный артефакт**, всё ниже
  опирается на него;
- `SudrfURLBuilder.srvNum` — поддержка экземпляра базы суда (по умолчанию `1`, поведение
  не изменилось), плюс пять тестов в `URLBuilderTests`;
- черновик заметок к релизу в `Docs/branch-changelogs/cassation-court-acts-database/`.

## Задача 1. Живая разведка — выполнена 06.08.2026

Прогон по всем девяти КСОЮ и Кассационному военному суду. Полная грамматика с числами —
`Docs/architecture/ksoyu-listing-grammar.md`; здесь только итоги по пунктам.

| Пункт | Итог |
| --- | --- |
| 1.1 Перечень работает | **Да.** Минимальный URL отдаёт `table#tablcont` и счётчик. Резервный «полный набор пустых полей» из `sudrfscraper` **не работает**: его ломает пустой `vnkod=`. Ещё нужен браузерный User-Agent — иначе WAF даёт 403 |
| 1.2 Пагинация | **Да.** 25 записей на страницу, глубина не ограничена (727 дел → 30 страниц, последняя с 2 строками) |
| 1.3 Диапазоны дат | **Все три работают и независимы.** Одно окно 01–07.06.2026 на 2 КСОЮ: поступление 727, рассмотрение 770, публикация 530. Префикс даты публикации свой у каждой картотеки: `G3_`, `U3_`, `P3_`, `ADM3_` |
| 1.4 Ссылка на текст | **Есть**, `name_op=doc&number=<id>&text_number=1`. Текст совпадает символ в символ с `cont_doc1` карточки → **один запрос на дело вместо двух** |
| 1.5 `srv_num` | **Риска нет.** На всех десяти судах `srv_num` игнорируется: 1 и 2 дают идентичный набор `case_id`. Несколько экземпляров базы бывают у судов с несколькими зданиями, у КСОЮ здание одно |
| 1.6 `delo_id` | **Спор по КАС был мнимый.** `43`/`p33_case` — КАС (`8а-…`), `2550001`/`adm33_case` — КоАП (`16-…`). Это разные картотеки, корпусу нужны обе. Нормализация `delo_id` не нужна: при верном `new` короткая пара `5&new=2800001` равноценна длинной `2800001`. Критичен `new`, не `delo_id` |
| 1.7 Капча | Капча у 1, 3, 4 и 6 КСОЮ; у 2, 5, 7, 8, 9 и `vkas` её нет. Картинка приходит инлайном (`data:image/png;base64`), `captchaid` — нонс на запрос, к cookie не привязан. **Не закрыто:** сколько страниц проходит на одной решённой паре — требует решения картинки |

Главная находка сверх плана — **тихий отказ**. `g33_case` и `u33_case` с `new=0` (или без
`new`) отдают 200 и «Данных по запросу не обнаружено», то есть кривой запрос неотличим
от честной пустоты. Харвестер обязан требовать наличие `table#tablcont` и сверять число
строк со счётчиком, иначе корпус молча недособерётся. Подробности — §5 грамматики.

## Задача 2. Правки в `SudrfKit` по итогам разведки — выполнена

1. ~~**`delo_id` кассации.**~~ **Закрыто без изменений кода.** Проверка показала, что
   короткие пары `5&new=2800001` и `4&new=2450001` работают на перечне (по поступлению
   и по публикации) и на поиске по номеру наравне с длинными — их эмитит сам сайт.
   Все четыре кассационные картотеки в реестре уже есть. Требование трёх внешних
   источников нормализовать `delo_id` оказалось необязательным.
2. ~~**Ротация `srv_num` в `SudrfClient`.**~~ **Закрыто без изменений кода:** пункт 1.5
   показал, что у КСОЮ параметр игнорируется. `SudrfURLBuilder.srvNum` остаётся ради
   судов других звеньев, клиентский перебор не нужен.
3. **Ссылка на текст в `ResultsParser`.** **Сделано.** Ссылки `name_op=doc` из строки
   выдачи разбираются в `CaseSearchResult.actTextLinks` (`CaseActLink`: `number`,
   `text_number`, ярлык, абсолютный URL). Поведение приложения не меняется — поле
   новое и никем пока не читается.

## Задача 3. Капча под Linux

Цель — рабочая модель в Python. По убыванию предпочтительности:

1. **Взять у друга `server/captcha-model.json`.** В присланный архив он не попал
   (`.gitignore`), как и корпус `data/captcha-training/`. Это веса CNN на чистом JS,
   а архитектура описана рядом в `server/captcha.js`:
   `CNN = { IW: 64, IH: 20, C1: 8, C2: 16, K: 3, PAD: 1, HID: 64, HEADS: 5, NC: 10 }`,
   forward-проход там же. Пять слоёв читаются в NumPy напрямую — ONNX не нужен вообще.
   Даёт сразу проверенные 97,9 % по цифрам.
2. **Переобучить** ту же архитектуру в PyTorch на объединённом корпусе (у друга
   `data/captcha-training/solved/`, у нас
   `~/Library/Application Support/Sudrf/captcha-training/solved-numeric/`) и выгрузить
   в ONNX. Сетка крошечная, полчаса на CPU.
3. Конвертация CoreML → ONNX — **не надо**: `coremltools` односторонний, конвертер
   в `onnxmltools` заброшен. Оставлено как последний вариант.

Обязательно перенести поведенческие правила (детали — в `ksoyu-listing-grammar.md`, §5):
портал держит одну капчу на IP по несколько минут и не меняет её от неверных ответов;
решённая пара действует на весь регион около 6 часов; у антибрутфорс-судов (известен
Краснодарский краевой) авто-сабмит накапливает блокировку на IP — для них только подсказка
человеку.

## Задача 4. VPS

Российский хостинг (Timeweb, Selectel и подобные). До написания кода проверить `curl`-ом
с сервера, что суды отвечают и TLS-цепочка собирается. Сертификаты Минцифры лежат
в репозитории: `Sources/SudrfKit/Resources/RussianTrusted*.cer`.

Риск считается низким: `sudtudtestbot` уже работает на Timeweb и ходит на суды обычным
`fetch` без специального CA-бандла.

Сбор лучше держать **отдельно от сервера с боевыми делами**: массовый обход рискует
репутацией IP, и ронять адрес, с которого юристы ведут дела, не стоит.

## Задача 5. Репозиторий харвестера

Отдельный проект (`practice-harvester`), Python: `httpx` + cp1251, `selectolax`/`lxml`,
`psycopg3`, `pydantic`, `alembic`, `sentence-transformers`, `FastAPI`.

Из этого репозитория ему нужны справочники — `CourtDirectory` (9 КСОЮ + Кассационный
военный суд) и `CartotekaRegistry`. Их стоит выгрузить в JSON скриптом
`Scripts/export-directories.swift` (ещё не написан), чтобы не поддерживать два списка руками.

Порядок работ: разведка → схема БД и слой сырья → перечень → тексты → индексы → API.
Раскатка судов: 2 КСОЮ (без капчи) → 3 КСОЮ (с капчей, боевая проверка модели) →
остальные, включая `vkas.sudrf.ru`.

## Источники знаний

| Источник | Что в нём |
| --- | --- |
| `Docs/architecture/ksoyu-listing-grammar.md` | Грамматика перечня, провенанс, чек-лист проверки |
| `sudrfscraper` (Java, опенсорс, ~2 года) | `src/main/resources/searchpatterns/*.properties` — эталонные URL; `strategy/SUDRFStrategy.java` — ротация page/srv_num; `strategy/GeneralParser.java` — разбор строки и текста |
| `sudtudtestbot` (Node, друга, разрешён) | `server/sudrf.js` — живой разбор ГАС 2026; `server/captcha.js` — CNN; `docs/captcha-solver.md`, `docs/captcha-retrain.md`; фикстуры `test/fixtures/2kas/`, `ksoyu-nonmoscow/` |
| Навык `sudrf-case-search` | Параметры картотек всех звеньев, правило cp1251, подводные камни |

## Что важно не потерять

- **262-ФЗ.** Часть актов не публикуется: пустая выдача не означает отсутствия дела.
  Дела без текста должны помечаться явно, иначе база выглядит полнее, чем есть.
- **Уважительный доступ.** Троттлинг не меньше 3 с на хост, ночное окно, дневной потолок
  запросов, `robots.txt`, User-Agent с контактом, лог всех запросов.
- **Правовой режим.** Пока это личный исследовательский инструмент, вопрос не стоит.
  Перед открытием доступа третьим лицам — отдельная проверка условий использования сайтов
  судов, режима повторного распространения и остаточных персональных данных.
