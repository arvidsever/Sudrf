# Sudrf roadmap

> Живой план работ. Обновляется сразу после мёрджа задачи, а не по особому запросу.

## Context

Развёрнут из `~/Downloads/Sudrf_roadmap_handoff.md`, сверен с кодом и скорректирован по
ходу работы. Покрывает все открытые issues, а не только названные в handoff.

Рабочий цикл: одна задача → PR → self-review → внешний review (Codex) → CI → мёрдж →
обновление локального main → **обновление этого файла** → закрытие issue → визуальная
проверка автором → добро на следующую. Промоушен changelog — последним коммитом в ветке
перед мёрджем.

**Этот файл — единственный адрес плана.** До 21.08.2026 план жил в `~/.claude/plans/`,
и это дважды сбивало с толку: файл там привязан к сессии, поэтому правки уходили на диск,
но в интерфейсе показывалась прежняя редакция. Сперва причину списали на Bash против
Write/Edit (ред. 3–5), на деле дело было в адресе файла. В репозитории этой проблемы нет:
план версионируется, виден в PR и не зависит ни от сессии, ни от кэша интерфейса.

Правки — обычным коммитом в ветке задачи, вместе с самой задачей.

Для чтения прямо в приложении план продублирован страницей:
https://claude.ai/code/artifact/6238bbc8-d733-4849-957a-bb183a384fe0 — это копия,
пересобираемая из этого файла после мёрджа. Источник правды всегда здесь.

## Сделано

| Версия | Issue | Что оказалось на деле |
| --- | --- | --- |
| 0.42.13 | #83 календарь | `minHeight` без потолка + `Spacer`, забиравший высоту колонки |
| 0.42.14 | #75 навигация | Капсула не резервировала место; пять экранов гадали отступ → `NavChrome.contentInset` |
| 0.42.15 | #97 сортировка судов | Номер уже лежал в `TerritorialCourt.number` |
| 0.42.16 | #99 ложные заседания | «Есть время ⇒ заседание» + дубль эвристики в ленте. Попутно убран `kind` из `feedID` |
| 0.42.17 | #100 суд инстанции | `nextEventCourt` в presentation. Попутно `recordCourt` — иначе ломался фильтр |
| 0.42.18 | #80 срок от итога | «Последняя строка с непустым результатом» → семантический триггер |
| 0.42.19 | #84 стадия апелляции | Две дыры словаря: возврат жалобы и голое «Прекращено» |
| 0.42.20 | #98 retention сроков | Один предикат вместо трёх копий; в календаре была своя копия условия |
| 0.42.21 | #85 шапка карточки | Стеклянная подложка — выбор автора из четырёх нарисованных вариантов |
| 0.42.22 | #102 карточки дел | Formatter был готов; правка — вёрстка + `categoryTail` |
| 0.42.23 | #96 слайдер навигации | `Glass.identity` + `glassEffectID`; акцент ушёл из подписи, остался на счётчике |
| 0.42.24 | #105 верхняя панель | Поиск глобален — уехал в полосу окна; сортировка потянула карточные виды |
| 0.42.25 | #103 настройки | Пять разделов, а не шесть: «Общие» нечем наполнить |
| 0.42.26 | правки тулбара | Кнопка обновления рисовалась пустой; капча ушла из строки меню; лаг |
| 0.42.27 | упаковка .app | Ресурсные бандлы не клались внутрь **никогда** — приложение падало |
| 0.42.28–30 | слайдер #96 | Три захода: скорость → механика → порядок слоёв |
| 0.42.31 | первый запуск базы | Диагноз не подтвердился: свежая установка работала, у тестировщика база БЫЛА |
| 0.42.32 | иконка (#146) | Артворк из Codex приехал без альфы — сплошной квадрат; пересобран по сетке macOS |
| 0.42.33 | Spotlight (#95) | Три причины: конфликт старых LaunchServices-регистраций, неполный набор fetch-атрибутов и сбой App Entity donation в macOS 27 beta |
| 0.42.34 | срок записей Spotlight (#153) | Явный бессрочный индекс и одноразовый rebuild старого v4-манифеста |
| 0.43.0 | Казначейство (#150) | Все листы из карточки суда, строгая RSS-привязка, история исполнения и независимый refresh |
| 0.44.0 | ФССП (#113), ручной контур | Точный поиск всех ИД по всем регионам, независимый refresh двух источников и ручная CAPTCHA без потери последнего успеха |
| 0.44.1 | лаборатория CAPTCHA ФССП (#160) | Отдельный developer-tool, FSSP-препроцессор 240×80, стабильный SHA-split и fail-closed bootstrap-обучение |
| 0.45.0 | CAPTCHA ФССП и СОЮ | Production-модель ФССП с тремя свежими попытками и ручным fallback; ансамбль двух новых числовых моделей СОЮ |
| 0.45.1 | #124 ложные заседания | Время и результат перестали продвигать строки движения; заседания определяются строгим allowlist поля `event` |
| 0.45.2 | TestFlight-сборка | Sandbox и миграция контейнера вошли в main; archive теперь гарантированно содержит все production-модели CAPTCHA |
| 0.45.3 | #118 сортировка мировых участков | Номер берётся из классификационного кода `NNMSNNNN`; общий порядок больше не сравнивает названия участков |
| 0.45.4 | #127 русская локализация | Удалены девять ошибочных английских переводов русских source strings |
| 0.45.5 | #112 судья в групповом календаре | Судья показан у каждого дела; общий footer оставлен для общих реквизитов |
| 0.46.0 | #114 удаление подборок | Подборки удаляются с подтверждением; сами отслеживаемые дела сохраняются |

### Сопоставление ранее собранных локальных версий

| Старое обозначение | Новая версия | Основание |
| --- | --- | --- |
| 0.42.35 | 0.43.0 | Новая пользовательская функция — Казначейство |
| 0.42.36 | 0.44.0 | Новый источник данных — ФССП |
| 0.42.37 | 0.44.1 | Внутренний инструмент обучения; production не меняется |
| 0.42.38 | 0.45.0 | Автоматическая CAPTCHA ФССП и новые production-модели СОЮ |

Main — **0.46.0**, билд 127, 767 тестов. Следующее исправление — 0.46.1; следующая новая пользовательская функция или источник данных — 0.47.0.

### Правила SemVer

- Новая пользовательская функция или новый источник данных повышают второй сегмент и сбрасывают третий в ноль.
- Исправления, косметические изменения, переобучение без новой продуктовой возможности и developer-only инструменты повышают третий сегмент.
- Каждая влитая ветка получает собственный номер, выбранный по этим правилам; отсутствие отдельной сборки не является основанием объединять заметки.

**ФССП #113 работает end-to-end с production-распознаванием CAPTCHA.** Для каждой строки
исполнительного документа поиск идёт независимо по опубликованному судом электронному ИД,
а при его отсутствии — по номеру бумажного листа; регион не ограничивается, чтобы не
потерять переданное производство. Точное совпадение хранит официальные поля ФССП рядом с
независимым результатом Казначейства. CAPTCHA и ошибка одного источника не стирают
последний успех другого. Автор визуально подтвердил живой сценарий
`11RS0001#2-9143/2025#1`: найдено ИП `587893/26/98078-ИП`, предыдущий номер и
подразделение совпали с официальной выдачей; второй ИД независимо получил «не найдено».

В 0.45.0 отдельная CoreML-модель ФССП прошла неизменённый production-gate: 4 000
подтверждённых сервером PNG, 30 вручную проверенных фикстур, 97,2% полных ответов на
независимом экзамене и 99,7% точности у 373 ответов с уверенностью не ниже 90%. Поток
получает свежий challenge на каждой из максимум трёх попыток, а затем оставляет ручной
fallback. Модель опубликована отдельным release asset; чистая CI-сборка проверяет SHA-256,
eligibility-отчёт и наличие модели внутри `.app`.

**Числовая CAPTCHA СОЮ в 0.45.0 переобучена на 16 228 визуально проверенных PNG.** Две
компактные CoreML-модели работают ансамблем: приложение выбирает более уверенный ответ.
На независимом экзамене полностью распознано 91,4% CAPTCHA и 98,2% отдельных цифр; все
известные регрессионные изображения распознаны без ошибок. Обе модели доставляются
отдельными release assets и вместе проверяются в упакованном приложении.

**#124 закрыт в 0.45.1 строгим allowlist заседаний.** Время и текст результата больше не
служат положительными признаками: общий предикат lifecycle, «Обзора», календаря и ленты
смотрит только на подтверждённые формулировки поля `event`. Результат по-прежнему нужен
только для исключения уже завершённых заседаний из будущих; миграция данных не требуется.

**#118 закрыт в 0.45.3 числовым порядком мировых участков.** `MagistrateCourt.number`
берёт номер из фиксированного классификационного кода (`11MS0001` → `1`), а
`SearchModel` передаёт его в уже существующий общий сортировщик. Участки идут как
№ 1, № 2, № 10, № 100; некорректные коды и ненумерованные суды сохраняют алфавитный
fallback.

**#127 закрыт в 0.45.4 удалением ошибочной английской локализации.** Девять русских
source strings в `Localizable.xcstrings` имели `en`-переводы, поэтому macOS показывала
`Open case` и другие английские подписи. Английские юниты удалены; каталог и development
region остаются русскими, без дублирования source strings и без лишнего `knownRegions`.

**#112 закрыт в 0.45.5 привязкой судьи к каждому делу в календаре.** В grouped-блоке
судья теперь показан компактной подписью в той же строке, что время и номер дела, а общий
footer содержит только общие реквизиты суда и помещения. Конфликтные и одиночные блоки
сохраняют прежний состав данных; высота блоков и позиции на временной сетке не изменены.

**#114 закрыт в 0.46.0 удалением пользовательских подборок.** Действие доступно в
контекстном меню подборки и всегда требует подтверждения с числом дел. Удаляется только
членство дел в подборке: сами дела остаются отслеживаемыми, активный фильтр возвращается
к «Все дела», счётчики перестраиваются, а Spotlight обновляется только для затронутых дел.
Системную строку «Все дела» удалить нельзя; пустые подборки удаляются тем же действием.

**TestFlight в 0.45.2 больше не является отдельной линией разработки.** Sandbox,
миграция прежнего контейнера и полный набор production-моделей CAPTCHA влиты в `main`.
Каждая следующая сборка для тестировщиков архивируется из актуального `main` с новым
номером build; постоянная ветка `testflight` не используется. Проверка Xcode-сборки
сверяет SHA-256 всех трёх моделей и eligibility-отчёт внутри фактического `.app`.

**Исполнение в Казначействе #150 завершено сквозным контуром.** Карточка суда извлекает
все строки таблицы исполнительных документов, общий блок «Исполнение» стоит в конце
карточки, а для отслеживаемых дел бумажные листы автоматически проверяются через RSS и
прямую страницу Казначейства. Сохраняются точный официальный статус, орган, подразделение
и история по устойчивым `guid`; not found, ambiguous, terminal и ошибки источника не
стирают последний успех и повторно проверяются по общему TTL. Электронные ИД и бумажные
листы, явно направленные приставам, уже показаны как ФССП, но не отправляются в
Казначейство. Live-кейс `2-8236/2025` подтвердил три независимых бумажных результата,
включая восемь этапов для `ФС № 049377165` (`documentId=2902695`).

**Spotlight в 0.42.33–0.42.34 проверен обоими путями.** Встроенный поиск возвращает дела по
«декабрист», а системный Spotlight видит Sudrf и показывает его записи при фильтрации по
приложению. Общая выдача ранжирует их ниже контактов и файлов — это системный рейтинг, не
потеря индекса. Production-путь теперь использует именованный `CSSearchableIndex` и
`CSSearchableItem` со связанным `IndexedEntity`: так записи остаются доступны во время
известного сбоя `indexAppEntities` / «Failed to request donation» в macOS 27 beta. Записи
получают `expirationDate = .distantFuture`, а переход с manifest v4 на v5 один раз полностью
перестраивает старый индекс, чтобы прежние записи тоже получили новый срок.

**Чужие PR проверять файлами, а не описанием.** В #146 CI был зелёный, описание
перечисляло проверки размеров и `actool` — а PNG не имели альфа-канала вовсе, и иконка
встала бы в доке прямоугольной плашкой. Сборка и подпись проходят с любой формой, поймать
это мог только `sips -g hasAlpha`. Заодно в PR не было промоушен-коммита — черновик
changelog так и остался в `Docs/branch-changelogs/`.
Релиз: https://github.com/arvidsever/Sudrf/releases/tag/v0.42.30 (первый после 0.40.0).

**Слайдер навигации занял три версии, и вот почему.** Ставка на стеклянный морфинг
(`glassEffectID` + `.glassEffectTransition(.matchedGeometry)`) не сыграла: он не перемещает
форму, а гасит её на старой вкладке и зажигает на новой. 0.42.28 замедлял ход — анимировать
было нечего. 0.42.29 перевёл слайдер на `matchedGeometryEffect`, он поехал, но
`GlassEffectContainer` собирает стекло потомков в один слой и кладёт его НАД содержимым,
из-за чего слайдер закрыл подпись. 0.42.30 убрал контейнер. Вывод: у Liquid Glass
семантика слоёв не такая, как кажется по названию API, — проверять сборкой, а не чтением.

**Стекло на стекле теперь есть и никуда не делось.** Трек и слайдер — два слоя без
контейнера, ровно то, что проект себе запрещал в `MyCasesView:44`. Автор принял вид, но
лекарство остаётся в одну строку: снять `.glassEffect` с трека.

Issue закрываю я, после мёрджа и обновления плана. Автозакрытие по телу PR не работает:
GitHub понимает только английские `closes`/`fixes`/`resolves`.

**Подтверждено визуально автором:** сортировка АСОЮ/КСОЮ (#97), суд нужной инстанции у
ближайшего события (#100), высота карточек календаря (#83), шапка выдачи (#75), уход
завершённых апелляций из стадии (#84), миграция `feedID` без ложных уведомлений (#99),
счётчик просроченных сроков (#98, 61 → 3).
Ручная CAPTCHA и два независимых результата ФССП (#113) подтверждены автором на живом
деле после выпуска 0.44.0.
Не проверен только #80 — нужно уголовное дело с приговором.

## Ждёт визуальной проверки

**#85 — стеклянная шапка карточки дела.** Внутри шапки лежат стеклянные кнопки и
стеклянный `ForceBadge`, то есть стекло на стекле. Если мутно — `GlassEffectContainer`
(`CalendarScreen.swift:117`); крайний вариант — откат к непрозрачной шапке.
Смотреть: кнопки и чип силы → стык с капсулой навигации → читается ли заголовок при
прокрутке. Макеты: https://claude.ai/code/artifact/d3e91e0a-ef82-444f-ab46-716f466b978b

**#102 — карточки дел.** Стороны первой строкой, номер корешком сверху, суд под
сторонами, категория свёрнута до последнего раздела; карточки в сетке должны стать одной
высоты. Открытый вопрос: **вид производства с карточки исчез** — корешок занял номер. В
«По производствам» это не потеря, вид назван в заголовке группы; в «По стадиям» и «По
подборкам» цветное слово было единственным быстрым признаком уголовного дела.
Макеты: https://claude.ai/code/artifact/e7ddc33d-2c83-427e-93ae-2d712cf4693b

**#96, #105, #103 — подтверждены автором** по ходу шести сборок: слайдер, кнопка
обновления, капча в настройках, лаг, порядок слоёв. Остались непроверенными частности:
экран «Поиск» с двумя полями рядом, узкое окно 1180 pt, ширина окна настроек 720 pt.
Макеты и решения: https://claude.ai/code/artifact/798650a1-703e-4a6e-bb73-cadeb01f25fe

## Что показал опыт

Восемь из тринадцати задач потянули находки, которых не было в issue. Трижды это был
фоллаут собственной правки (смена `feedID` воскрешала прочитанные записи ленты; смена
подписи суда ломала поиск по суду первой инстанции; retention сроков заставлял колонку
врать «все сроки подтверждены») — чинилось в том же PR. Дважды — соседний дефект,
вынесенный отдельно (#118, #124). В #102 находка пришла из фикстур: последний раздел
рубрикатора категории часто оказывается заглушкой («иные…», «прочие…»), и наивная свёртка
до последнего раздела дала бы «иные споры по делам о восстановлении на работе» вместо
«Дела о восстановлении на работе». Разделителя в живых карточках два — `→` и `->`.

**Пять раз диагноз расходился с фактом.** В #84 кейс 2 ломался не по описанной причине;
в #104 discovery уже реализован; в #128 моё собственное предположение про материалы автор
опроверг; в #127 строка не «не переведена» — наоборот, переведена; в #143 гипотеза «свежему
пользователю нужна база для миграции» не подтвердилась — пять тестов показали, что первый
запуск работает, а скриншот сам себя опроверг: путь резервной копии в нём появляется только
когда файл базы существовал. Вывод: воспроизводить до правки, не доверять формулировке
причины — и своей догадке тоже.

**Дважды мой собственный вывод оказывался неверен, и оба раза его опроверг автор.**
Про место плана (Bash против адреса файла) и про Liquid Glass («стеклу нечего
преломлять» — у Calendar под капсулой тоже ровный фон, и это не мешает). Проверять
референсом, а не рассуждением.

---

## Ближайшая очередь

### #101 + #133 Стороны в уведомлениях и в «Обзоре» — одним заходом

Источник — `AppRouter.clientName` (`AppModel.swift:1310`): предпочитает название подборки
именам сторон, отсюда `Импорт 03.07.2026` в заголовке. `FeedEntry.client` показывается в
ленте Обзора и участвует в поиске — семантику не трогать, добавить `partiesShort` и
`courtTitle`, суд брать из сессии, как в #100.

**#133 — та же правка, третье место применения.** В «Обзоре» стороны есть только в левой
колонке («Заседания»); «Сроки обжалования» (`OverviewView.deadlineBody`, `:236`) и «Лента
изменений» (`:314`) показывают номер и тип события. Formatter после #102 готов и обкатан,
трогается только вёрстка двух колонок. Делать вместе с #101: источник строки один, а
разнести — значит второй раз решать, что писать вместо подборки.

### Блок сроков — #129 + #125 + #111 одним заходом

Оставшееся в блоке — арифметика, и её надо делать одним заходом, иначе единица измерения
срока вводится трижды:

- **#129 месяцы считаются как 30 дней** — `appealDays` возвращает 30/90, `daysPhrase`
  переводит их обратно в «1 месяц»/«3 месяца», `DateUtil` метода прибавления месяцев не
  имеет вовсе. Ошибка знакопеременная: от 18.08 занижение на день, от 31.01 завышение на
  два-три — а завышенный срок опаснее всего. **Самый дешёвый и самый массовый из трёх.**
- **#125 частная жалоба** — определение о возврате (`9-…`/`9а-…`) обжалуется частной
  жалобой: 15 **рабочих** дней (ГПК ст. 332 + ст. 107 ч. 3, КАС ст. 314 + ст. 92 ч. 2), а
  код выставляет 30 календарных и называет это апелляционной жалобой на решение.
- **#111 нерабочие дни** — перенос окончания срока с выходного.

Связка #125 и #111 существеннее, чем казалась: #111 просит **переносить** срок с
выходного, #125 требует **исключать** нерабочие дни из счёта. Механизмы разные, календарь
общий — заводить его дважды бессмысленно.

Осторожно: правило зависит от кодекса. Месяцы календарные; дни по ГПК/КАС — рабочие; КоАП
(ст. 4.8) и УПК (ст. 128) переносят окончание, а не исключают из счёта. Механически
переносить правило ГПК нельзя.

**#128 — за #70.** Срок апелляции выставляется, когда итогового акта в движении нет
(дело `2-6027/2026`: основанием стало «Иск принят к производству»). Причина — фолбэк,
сохранённый в #80 сознательно: `resolveTerminalFirst` завершает дело при отсутствии срока
апелляции, поэтому отказ от срока увёл бы дело в «Завершённые». Чинить только после
развязки стадии и срока в #70.

### #144 База не открывается — дать выбор вместо тупика

Заведена по следам краша у тестировщика. Сейчас «база есть, но не открывается» —
конец работы: `StorageStartupFailureView` (`RootView.swift:205`) советует закрыть
приложение и разбираться с файлами вручную. Нужны две кнопки: «Попробовать ещё раз»
(без операций с файлами, закрывает временные причины) и «Отложить базу и начать заново»
(карантин по явному нажатию).

**Главное требование — обратный путь.** Отложить мало, надо уметь вернуть: читаемое имя
папки вместо UUID, `README.txt` с инструкцией рядом, все три файла (`.store`, `-wal`,
`-shm`) вместе, ничего не удалять. Раскладка карантина уже есть в
`SudrfPersistentStoreBackup.prepare` (`DataCatalog.swift:206`) — переиспользовать.

Осторожно: блокировка из v0.41.0 не перестраховка. Временная неисправность даёт тот же
симптом, что и повреждение, поэтому карантин — только по явному нажатию и никогда сам.

При разборе проверить, не та же ли это причина, что в #77 (пустая база после обычного
перезапуска).

---

## Требует решения автора

**#77 и #44** — предлагаю поднять вперёд, не дожидаясь блока надёжности: #77 показывает
пустую базу после обычного перезапуска, #44 рапортует «обновлено» при откате транзакции.
Оба про сохранность данных. Отклонение от порядка handoff — решение за автором.

## Архитектурные решения автора — 23.08.2026

**#155 — поднять в высокий приоритет.** После ближайшего блока correctness/data-safety,
не дожидаясь закрытия всего накопленного UI/reliability backlog, реализовать #155 в shadow
mode. Следующие крупные функции по возможности строить уже поверх event journal, а не
размножать текущие независимые эвристики изменений.

**#148 Apple Calendar — первая версия только Sudrf → Calendar.** Судебное состояние и
`CaseEvent` остаются source of truth. Изменение или удаление `EKEvent` пользователем не
меняет snapshot/CaseEvent; Calendar — проекция. Двустороннюю синхронизацию в первый scope
не включать. Если позже понадобятся пользовательские заметки/override, хранить их отдельным
локальным слоем, не смешивая с данными суда.

**#149 сервер + APNs — обязательная часть продукта, но позже.** Это не условная задача
«если локального background refresh окажется мало»: always-on backend точно строим, но
только после стабилизации локального event contract #155 и базовой source reliability.
Сервер не заводит вторую доменную модель: он воспроизводит/потребляет те же normalized
snapshots и semantic `CaseEvent`, а затем масштабирует scheduling/delivery.

---

## Дальше по приоритету

### Дешёвое и заметное
**#91** явное «Убрать из отслеживания» · **#117** вместо
неопубликованного акта показывается страница суда · **#92** добавление дела по ссылке ·
**#110** отчёт импорта CSV ·
**#151** явные кнопки открытия карточки на сайте суда.

### Доверие к данным
**#56** ложный срок при живой кассации · **#86** КоАП: статус после оставления без
изменения · **#76** облсуд: пропуск апелляции · **#94** номер по предыдущей регистрации ·
**#93** вкладка «Обжалование» · **#74** движение по материалам.

**#90 + #130 — КоАП-надзор, одна задача.** #90 просит завести стадию «Надзор» и активное
звено КСОЮ; #130 показывает тот же дефект с другой стороны — надзорная жалоба по КоАП
попадает в фильтр «Кассация». В КоАП кассационной стадии нет вовсе: первая инстанция →
апелляция → надзор (КСОЮ и ВС РФ). Чинить одним заходом, иначе модель стадий КоАП
описывается дважды.

**#132 КСОЮ: номер `8-` не сменяется на `88-`.** После определения о принятии жалобы к
рассмотрению КСОЮ присваивает новый номер производства, карточка продолжает показывать
номер поступившей жалобы. Родня #94 (номер по предыдущей регистрации) и #89 (карточка
КСОЮ без движения) — все три про то, что у дела в КСОЮ больше одного номера.

**#70 rules engine** — зонтик над #56/#111/#125/#128. В нём же развязать стадию и
расчётный срок: сейчас `resolveTerminalFirst` завершает дело при отсутствии срока
апелляции (разбор — в комментарии к #70). Делать после того, как блок сроков накопит
кейсы; фикстуры #80 годятся как негативные. Rules engine остаётся владельцем юридической
формулы/provenance, но downstream deadline events должны использовать contract #155.

### Полнота цепочки
**#87** фоновое обнаружение вышестоящей инстанции · **#156** поиск дела после передачи по
подсудности через `name_op=r_juid` · **#89** карточка КСОЮ без движения · **#104** discovery
ВС РФ — **не greenfield**, уже подключён к refresh (`Movement.swift:648-664`), это отладка ·
**#165** прямой поиск уголовных дел в источнике ВС РФ: отдельно проверить категории,
нормализацию номера и отличие честного отсутствия результата от неподдерживаемого маршрута.
⚠️ `vsrf.ru` недоступен из песочницы, нужны HTML-фикстуры от автора: страница поиска по
`2-8236/2025`, карточка `3-КФ26-187-К3` и конкретный уголовный кейс для #165.

`r_juid` считать **evidence source, а не источником истины**: registry по известному УИД
может подсказать карточку другого суда/новый номер, но auto-link допускается только после
identity/reconciliation. Один УИД + новая строка registry сами по себе не доказывают
«передачу по подсудности». Парсер registry лучше делать generic, чтобы тот же сигнал мог
помочь #87, но первая product-функция #156 остаётся узкой и проверяемой.

### Надёжность
**#82**, **#88**, **#164** CAPTCHA · **#79** ложный «неизвестный формат» · **#78** мировые
«ничего не найдено» · **#81** военные суды · **#43** несвежая сводка · **#45** рендер актов ·
**#46** App Intents на холодном старте · **#64** persistence commits.

**#164 — CAPTCHA мировых судей `*.msudrf.ru` является отдельным session protocol, а не
вариантом `sudrfToken`.** Эмпирика уже лежала в `changelog-v0.39.15.md`: `kcaptchaForm`
отправляет POST `captcha-response`, картинка загружается с `/captcha.php`, `captchaid` нет,
а успешность хранится в cookies. Manual WebView-flow уже работает; нужно добавить
auto-unlock. Предпочтительный путь после open-source review — HTTP-first: GET challenge и
PNG, OCR `.kcaptcha`, POST ответа в той же cookie jar, затем повтор исходного поиска;
скрытый `WKWebView` оставить fallback, только если live-вариант реально требует браузер.
Не синтезировать фиктивный `captchaid`: в общей CAPTCHA-модели различать token challenge и
session challenge/session-unlocked result.

Для source reliability общий invariant: HTTP 200 ещё не означает usable snapshot.
CAPTCHA, maintenance/stub, partial parse и временная пустая выдача должны сохранять
последний успешный state и отдельно фиксироваться как refresh attempt; они не могут
порождать ложное «удалено/отменено» и не должны делать карточку свежей на полный TTL.

### Инфраструктура
**#155** единый `CaseSnapshot → semantic CaseEvent` contract: **высокий приоритет после
ближайшего correctness/data-safety блока**; сначала shadow journal и snapshot→events
fixtures, затем перевод ленты/локальных уведомлений на него. Это опора для #70/#148/#149,
а не backend-задача сама по себе · **#69** Developer ID + notarization: сертификат создан,
Developer ID export и hardened runtime проверены в #95; остались notary credentials,
отправка, stapling и release job · **#149** сервер + push — обязательный поздний этап после
#155/source reliability · **#68** Source Health · **#65** canary · **#66** APPLE-6 · **#67**
live-case session из `AppRouter`.

### Новые источники — последними
**#106** Москва → **#107** СПб → **#108** общий модуль. ФССП #113 уже подключена поверх
общего блока исполнения; production-gate её отдельной CAPTCHA-модели снят в 0.45.0.
Новый source adapter для ФССП не требуется.

---

## Архитектурная дорожка из open-source review

Подробная фиксация: `Docs/architecture/open-source-reference.md` и
`Docs/architecture/reference-alexxmirny-sudrf-parser.md`.

Главный вывод — **не менять стек**, а сделать явными контракты между уже существующими
слоями. Целевая цепочка:

`transport → source adapter → normalized snapshot → identity/reconciliation → semantic diff → append-only CaseEvent journal → projections`.

### Архитектурные invariants

- **Swift/SwiftUI/SwiftData остаются.** SwiftData не менять без измеренного bottleneck;
  сначала добавить логические сущности snapshot/event/refresh-attempt и нормальные
  миграции.
- **HTTP-first.** `URLSession` + cookies/session continuity + CP1251 + trust roots +
  throttle/retry/backoff — штатный transport. Selenium/Puppeteer/browser automation —
  только fallback для источника, который объективно нельзя получить обычным HTTP. Для
  `msudrf` #164 это означает сначала реализовать KCAPTCHA как HTTP session-unlock, а не
  сразу переносить Puppeteer/WKWebView-подход референсов.
- **Transport не знает процессуальной семантики.** Его ответственность: HTTP, redirects,
  charset, TLS, cookies, throttling, retry, CAPTCHA continuation, host health. CAPTCHA
  protocol при этом может быть token-based или session-based — транспорт обязан сохранить
  это различие, а не свести всё к фиктивному `captchaid`.
- **Source adapter не знает UI.** Он строит URL, классифицирует response, извлекает
  source-native identity и нормализует listing/detail в общую модель. По мере добавления
  #106/#107/#108 вводить capabilities/adapter registry вместо разрастающихся `if source`.
- **Domain не зависит от presentation.** Identity/reconciliation, стадии, сроки и semantic
  diff живут вне View/notification formatter.
- **Номер дела — атрибут, не primary key.** Court identity — официальный код, где он
  надёжен; карточка/производство — УИД либо source-native ID, scoped по court/source;
  logical case — граф связанных карточек. #94/#132/#156 — обязательные regression cases.
- **Последний хороший snapshot не стирается плохим fetch.** HTTP 200 stub, CAPTCHA,
  partial/unknown response и parse error — отдельный refresh outcome, а не новое пустое
  состояние дела.
- **Raw JSON diff запрещён как пользовательская семантика.** `alexxmirny/sudrf_parser`
  полезно подтверждает модель snapshot→diff→notification, но его positional array diff
  — negative reference: перестановка строк/косметика не должны создавать события.
- **Все downstream-проекции читают один event contract.** Лента, локальные notifications,
  badges, Spotlight, Calendar и будущий APNs не сравнивают snapshots самостоятельно.

### Фазы и gates

**A. Надёжный source state — до semantic events.**

Закрыть/стабилизировать source correctness, identity и partial-state кейсы, которые могут
сделать новый snapshot ложным: #94/#132, #82/#88/#89/#164, #79/#78 и связанные фикстуры.
Не требуется ждать всех UI/reliability issues; нужен именно пригодный normalized input.

Для `msudrf` gate включает не только распознавание `kcaptchaForm`, но и проверяемый
continuation contract: challenge page → `/captcha.php` → POST `captcha-response` →
session unlocked → исходный listing, всё в одной cookie session. CAPTCHA failure не равна
нулевой выдаче.

Gate: один и тот же response детерминированно даёт либо usable normalized snapshot, либо
явный partial/error outcome; не существует пути «плохой HTML → пустой valid snapshot».

**B. Identity/reconciliation и discovery evidence.**

Использовать official court code + УИД/source-native IDs и хранить смену display number как
историю identity, а не как новое несвязанное дело. #156 добавляет `r_juid` как независимый
registry/evidence source для передачи по подсудности; #87 использует те же reconciliation
правила при фоновом discovery. Manual и periodic discovery обязаны сходиться к одной цепочке.

Gate: повторное обнаружение той же карточки другим маршрутом не создаёт дубль; новая
карточка с новым номером может быть связана без опоры на строковое равенство номера.

**C. #155 — shadow `CaseSnapshot → CaseEvent`.**

**Приоритет подтверждён автором 23.08.2026:** эту фазу брать сразу после ближайшего блока
correctness/data-safety, не дожидаясь закрытия всего UI/reliability backlog.

Сохранять последний normalized snapshot и append-only journal, но первое время не менять
действующую ленту. На каждом refresh считать semantic events параллельно и сравнивать с
текущим поведением. Event ID стабилен между relaunch/refetch и пригоден для дедупликации
локального и будущего серверного notification path.

Gate: identical snapshot → 0 events; reordering/whitespace/publish timestamp → 0 events;
новое заседание/акт/инстанция → ровно одно событие; partial fetch → 0 ложных cancel/delete.

**D. Event journal становится источником пользовательских изменений.**

После shadow-сверки перевести на journal ленту и локальные notifications, затем badges и
прочие производные. Старые presentation strings не участвуют в event identity. Историю
не пересобирать заново при изменении formatter, иначе старые уведомления «воскреснут».

**E. #70 подключается как юридическая семантика поверх того же contract.**

Rules engine владеет формулой срока, trigger/evidence и provenance; #155 владеет общей
семантикой события/dedup/projections. Deadline lifecycle (`proposed/confirmed/changed/
expired/superseded` — финальный vocabulary определить в #155/#70) не заводит отдельный
feed-specific ID.

**F. #148 Calendar — односторонняя projection, не второй database.**

**Решение автора 23.08.2026:** первый scope — только Sudrf → Apple Calendar. После event
identity маппинг `CaseEvent/event state ↔ EKEvent` становится технически прямым: перенос/
отмена обновляют существующий Calendar event. Обратное изменение `EKEvent` не меняет
судебное состояние в Sudrf; двустороннюю синхронизацию в этот этап не включать.

**G. Refresh health, scheduler и canary — после устойчивой семантики state/event.**

#87/#68/#65 должны различать `lastAttempt` и `lastSuccess`, не считать partial успешным
refresh и давать evidence, почему источник не обновился. Только после этого имеет смысл
адаптивный scheduler: приоритет по близости заседания/срока, свежим изменениям, ожиданию
higher instance, предыдущим ошибкам, TTL и недавнему открытию пользователем; для внешних
сбоев — exponential backoff + jitter и при необходимости host circuit breaker.

Отдельный scheduler issue заводить только если простой TTL после #87/#68 реально создаёт
starvation/лишнюю нагрузку; не строить очередь ради архитектурной красоты.

**H. Новые source families — поверх contract, а не рядом с ним.**

#106 Москва → #107 СПб → #108 общий модуль. Новый источник обязан дать capabilities,
source identity, normalized snapshot и fixture suite; UI не получает новый `if Moscow`
или `if magistrate`, если различие можно выразить adapter capability.

**I. #149 backend — обязательный поздний этап того же contract.**

**Решение автора 23.08.2026:** always-on backend и APNs точно входят в продукт, но позже.
После стабилизации #155 и базовой source reliability сервер масштабирует ingestion/
scheduling/delivery, не вводя вторую модель дела и второй diff-engine. Local refresh и
backend должны выдавать совместимые event IDs/semantics, чтобы одно изменение не приходило
пользователю дважды. PostgreSQL/Redis/queues/search index появляются только при серверной
нагрузке, которая их обосновывает; в desktop их нет.

### Fixture/test contract

Для каждого важного portal family нужны **два независимых уровня regression fixtures**:

1. `HTML/response → normalized snapshot`;
2. `old snapshot + new snapshot → expected CaseEvent[]`.

Постоянный pathology corpus: CP1251; canonical/alternate host; CAPTCHA и post-CAPTCHA
session; **`msudrf` KCAPTCHA (`kcaptchaForm` + `/captcha.php` + POST + cookie continuity)**;
maintenance/error page с HTTP 200; honest zero-results против unknown; duplicate rows;
missing/unstable UID; renumbering; partial KSOYU; stale links; vintage SUDRF;
Moscow/non-standard magistrates; `r_juid` с уже известной карточкой, новой карточкой и
временной пустой/ошибочной выдачей.

#65 live canary ловит внешний drift; fixtures не дают сломать уже исправленное поведение
при адаптации к этому drift. Для semantic event rule обязательны positive **и negative**
fixtures: ложное юридически значимое событие опаснее пропущенного cosmetic change.

### Что брать из reference projects

- **Juriscraper** — adapter/fixture discipline и отделение scraper от persistence/app.
- **CourtSniffer** — актуальные SUDRF protocol quirks, official court code, adapter registry,
  CAPTCHA/session observations; для `msudrf` особенно ценен принцип сохранения browser/
  HTTP session после CAPTCHA. Не переносить Puppeteer/RuCaptcha/TLS bypass как default —
  #164 сначала проверяет тот же протокол чистым HTTP.
- **tochno-st/sudrfscraper** и **dataout-org/sudrfparser** — corpus/legacy oracle, не stack.
- **OlegSirik/sudrf-proxy** — `discovery → local snapshot → refresh → diff → changes` как
  product mental model.
- **alexxmirny/sudrf_parser** — свежий минимальный `fetch → snapshot → diff → changelog →
  notification`, `r_juid` и content/header-based parsing; raw field/index diff и URL-hash
  identity использовать только как антипример.
- **CourtListener** — future server-scale ingestion/workers/search reference, только для #149.

### Явно не делать

- не replatform-ить рабочий native client на Python/Node/Java;
- не делать browser automation штатным транспортом обычных `sud_delo` страниц;
- не отключать TLS verification ради проблемного суда;
- не считать CAPTCHA/error/unknown HTML за `0 results`;
- не использовать human case number или URL как identity;
- не строить feed/Calendar/server notifications через независимые diff-алгоритмы;
- не переписывать SwiftData до измеренной проблемы;
- не тащить Elasticsearch/Redis/Celery/queue infrastructure до server-scale workload;
- не ставить LLM/AI в critical path процессуального статуса, identity или срока.

## Рабочие договорённости

- Одна задача = одна ветка = один номер версии (`AGENTS.md`).
- TestFlight собирается из актуального `main`; отдельную постоянную ветку для дистрибуции
  не вести.
- Промоушен changelog — последним коммитом в ветке перед мёрджем.
- План — `Docs/roadmap.md` в репозитории; обновляется в той же ветке, что и задача.
- Ключевое слово закрытия в PR писать по-английски (`Closes #N`), иначе issue не закроется.
- Воспроизводить дефект до правки: формулировка причины в issue четырежды оказывалась
  неверной, включая одну мою собственную догадку.
- Чужие осознанные решения (комментарии «почему так») не откатывать молча — выносить на
  решение автора.
- Находки self-review: фоллаут собственной правки — чинить в том же PR; соседний дефект —
  заводить issue.
- Визуальные решения не угадывать по коду: рисовать варианты и давать выбрать.
- При каждом обновлении плана перечитывать **весь** список открытых issues, а не только
  те, что уже стоят в очереди: автор заводит новые по ходу, и они не попадают в план сами.

## Verification

```bash
swift build && swift test
```

766 тестов, 5 пропущены, ошибок нет; обязательный набор проходит. Визуальные правки тестами не ловятся:

```bash
xcodegen generate && bash Scripts/make-app.sh
```

`Scripts/make-app.sh` не исполняемый — через `./` не запускается, только `bash`.
