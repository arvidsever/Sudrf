# v0.58.31 — Сохранение отчётов фонового обхода

Исправлен установленный дефект первого этапа #68: общий FIFO HTML-диагностики
удалял `refresh-walk_*.json` после успешного многочасового обхода.

- `SearchDiagnostics` ограничивает лимит 50 только собственными HTML/PNG-дампами.
- JSON фонового обхода и текстовые CAPTCHA-кандидаты не учитываются в этом лимите
  и не удаляются при потоке ответов неизвестного формата.
- Регрессия проверяет сохранение `refresh-walk` рядом с 51 HTML-файлом.

Порядок очереди, TTL, retry-политика, схема хранения и пользовательский интерфейс
не изменены. #68 и #180 остаются открытыми до повторного реального baseline.
