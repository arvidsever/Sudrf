# Captcha/opencode branch changelog drafts

These files were originally drafted as `changelog/changelog-v0.38.*.md` on the
captcha/opencode line. They are branch-local drafts until that branch becomes a
released line.

There was no standalone `changelog-v0.38.1.md` in the source branch; the copied
set matches the files that existed there.

Do not put these files back into the public `changelog/` directory until merge
or release time. At that point, choose the final version number according to the
actual release order and update `MARKETING_VERSION` / `CURRENT_PROJECT_VERSION`
with the public changelog.
