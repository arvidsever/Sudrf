# v0.39.32 — Tracking and deadline correctness (draft)

> Branch draft. Final release version is assigned only during merge/release.

- Removing a tracked case now uses its stable record key, so equal case numbers
  from different courts cannot remove each other.
- Cassation deadlines require an actual legal-force or appeal date.
- The notification delegate is restored at app launch without prompting.
