# v0.57.4 — основное лицо в делах КоАП

Черновик ветки `codex/koap-primary-party-239`; номер уточняется перед merge.

- Краткое название дела КоАП выбирает привлекаемое лицо по опубликованной роли,
  даже если защитник указан первым. Несколько лиц перечисляются компактно;
  защитник и представитель остаются в полной карточке участников.
- Если привлекаемое лицо не опубликовано, сохраняется имя другого участника
  с явной ролью. Статьи не подставляются к имени чужого участника.
- Сохранённое представление сторон исправляется из имеющегося движения при
  подготовке хранилища без сети и без пересчёта сроков, журнала или времени обновления.

## Проверка

- Xcode beta / Swift 6.4: полный `swift test` — 1176 тестов, 5 штатно
  пропущены, ошибок нет. Включены HTML-регрессия порядка участников, варианты ролей,
  явные колонки, несколько лиц, fallback и исправление сохранённых снимков.
- Подписанная Debug-сборка `xcodebuild` прошла; готовая копия:
  `/tmp/sudrf-239-derived/Build/Products/Debug/Sudrf.app`.
- Подготовка старого снимка проверена на сохранение остальных полей snapshot,
  исходного movement, времени успешного обновления, журнала и идентичности;
  повторная подготовка не записывает изменений. Нет движения или оно повреждено —
  прежний снимок остаётся без изменений.
- Проверен существующий путь `snapshot.partiesShort → clientName → FeedNotifier`;
  приоритет подборки сохранён, расширение содержания уведомлений остаётся в #101.
- Self-review и дополнительная проверка завершены без обязательных замечаний.
  `git diff --check` прошёл; пользовательская локализация не изменена.

8 сентября 2026 года прямой HTTPS-запрос исходной карточки `16-5133/2026`
(3 КСОЮ, `case_id=25004449`) вернул HTTP 403. Живая проверка карточки по этому
запросу не выполнена; результат не выдаётся за подтверждение исправленного интерфейса.
