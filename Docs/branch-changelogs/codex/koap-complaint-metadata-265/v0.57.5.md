# v0.57.5 — результат жалобы КоАП без строки УИД

Черновик ветки `codex/koap-complaint-metadata-265`; финальный номер назначается перед merge.

- Реквизиты карточки жалобы КоАП читаются и без строки судебного УИД: дата и
  результат рассмотрения больше не теряются из-за отсутствия этой строки.
- Завершённая жалоба выходит из активного «Надзора» после штатного обновления.
  Данные нижестоящего суда не подменяют результат жалобы; неоднозначные реквизиты
  не становятся основанием для уверенного завершения.
- Правила стадий и схема базы не меняются. Истребование дела и датированные
  строки рассмотрения остаются отдельной задачей #256.

## Подтверждение

8 сентября 2026 года сопоставлены живые карточки 16-3568/2021 (3 КСОЮ) и
16-5035/2023 (2 КСОЮ) с памятью работающего приложения. Суд публикует
«Возвращено без рассмотрения», но приложение сохраняло только поступление.
Происхождение и хеши HTML-примеров: `Tests/SudrfKitTests/Fixtures/ksoyu_koap_returned_provenance.md`.

## Проверка

- Профильные тесты: 95, без ошибок.
- Полный прогон окончательного кода через Xcode beta: 1183 теста, 5 пропущены,
  0 ошибок (SudrfKit 497, SudrfApp 601, FSSPCaptchaLab 10, CaptchaSolver 75).
- Debug-сборка Xcode beta: BUILD SUCCEEDED; отдельный каталог
  `/tmp/sudrf-265-derived`, версия выпуска не изменена.
- После штатного завершения прежней сборки запущена проверенная сборка.
  Обе карточки обновлены обычным открытием. В каждой появился результат
  «Возвращено без рассмотрения»; обе найдены в фильтре «Завершённые».
  «Надзор»: 7 → 5 строк, завершённые: 340 → 342.
- При перезапуске до открытия этих карточек общий счётчик изменился 544 → 481,
  КАС: 230 → 167. Это отдельное наблюдение запуска, не результат исправления
  метаданных КоАП; причина требует сопоставления записей до и после запуска.
- Локализация пользователя сохранена без изменений. Issue #265 остаётся
  открытой до merge; соседняя #256 не включена.
