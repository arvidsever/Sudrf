# A12 — независимость CaptchaSolver от SudrfKit

## Исправлено

- Удалена неиспользуемая зависимость `CaptchaSolver` от `SudrfKit`.
  Модуль solver остаётся самостоятельным, как предусмотрено архитектурой.

## Проверка

- `swift build` успешно собирает пакет без этого ребра зависимостей.
- В `Sources/CaptchaSolver` отсутствуют импорты `SudrfKit`.
