# v0.39.33 — App interaction and import lifecycle (draft)

> Branch draft. Final release version is assigned only during merge/release.

- Hidden search content is disabled outside Search, preventing invisible Return-key searches.
- Cancelled import workers are generation-gated and cannot clear or overwrite a newer import.
- Calendar regression coverage now verifies that sequential hearings do not overlap.
